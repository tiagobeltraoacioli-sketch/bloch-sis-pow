{
  "name": "bloch-testnet-faucet",
  "version": "0.1.0",
  "private": true,
  "description": "Reference testnet-only faucet for Bloch (bloch1t… addresses). SCAFFOLD, unaudited, pre-production. Test BLCH has no value.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "bloch-faucet": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "selftest": "tsc && node dist/selftest.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0"
  }
}
