name = "bloch-explorer"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# Configure BLOCH_RPC_URL in the Pages environment before deployment.
# Require a qualified HTTPS endpoint with a valid certificate and read-only
# upstream policy. No plaintext or public wildcard-DNS fallback is committed.
# Missing/invalid configuration returns 503; this patch does not provision TLS,
# DNS or origin firewall rules. See RPC-SECURITY.md for rollout requirements.
