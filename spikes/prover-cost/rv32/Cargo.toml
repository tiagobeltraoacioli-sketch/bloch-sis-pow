[package]
name = "rv32-mldsa-verify"
version = "0.1.0"
edition = "2021"
[workspace]
[dependencies]
fips204 = { version = "0.4", default-features = false, features = ["ml-dsa-65"] }
[profile.release]
opt-level = 3
panic = "abort"
lto = true
