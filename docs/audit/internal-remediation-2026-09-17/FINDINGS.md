# Internal audit remediation ledger — 2026-09-17

Audited source: `562e220`. Remediation base: `b066e3c`. Branch: `fix/internal-audit-20260917`.

All 200 findings are retained, including duplicates. IMPLEMENTED means code changed in this branch, not deployed, externally audited, or universally resolved on the live fleet. PARTIAL is not closure. No production binary was published.

IMPLEMENTED: 50, PARTIAL: 58, UNARMED CANDIDATE: 1, PROTOCOL DECISION: 4, BASE CHANGED: 7, OPEN: 79, REFUTED IN AUDIT: 1.

| Finding | Status | Evidence / remaining work | Original title |
|---|---|---|---|
| EN-01 | IMPLEMENTED | Oversized admission refusal plus continue-on-nonfitting selection; regression tests. | One admissible transaction censors every transaction on the network (`select_transactions` breaks on the first over-cap entry) |
| EN-02 | IMPLEMENTED | 16 MiB encoded-payload budget in addition to count; cached byte accounting. RSS includes extra object overhead. | Mempool is bounded by count (4,096) but not by bytes: ~28 GB per node from one unauthenticated peer |
| EN-03 | PARTIAL | Base already filters pre-start duties; held-attestation release now runs the same doppelganger check. Not a durable anti-cloning mechanism. | Doppelgänger protection halts a validator permanently when its *own* pre-restart attestation is replayed to it |
| EN-04 | IMPLEMENTED | Cached source counts and transaction identity index replace repeated mempool hashing. | `on_transaction` runs an O(mempool) SHA3 scan *before* any cheap refusal: ~40 ms of consensus-thread CPU per 120-byte frame |
| FC-01 | UNARMED CANDIDATE | All-zero consensus-roster fallback and regression exist behind u64::MAX. Not a live fix; needs end-to-end finality/partition/reward qualification and coordinated activation. | Post-e1400, ~60 epochs without any included attestation leaks every validator to zero and the chain dies permanently (no proposer can ever be drawn) |
| FC-02 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | Genesis-cohort cap hands a supermajority to the first minimum-bond independent validator once the taper is deep |
| FC-03 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Unauthenticated legacy `Exit` (tag 0x03) is consensus-valid: one scheduled proposer can retire the other 63 validators and own the roster after 32 epochs |
| INF-02 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | One credential class controls the majority of validator keys — HIGH — status: KNOWN (`deploy/SSH-ROLE-SEPARATION.md`, FLAG-DAY-EPOCH-800 "Known gaps")… |
| NET-01 | PARTIAL | RPC per-IP/queue bounds and more snapshot-served reads. Remaining methods still run on consensus thread; no production exposure changed. | Public bootnodes expose the full unauthenticated RPC (incl. `sendrawtransaction`) on `:8080`, and RPC executes on the consensus thread |
| NET-02 | IMPLEMENTED | Ownership, existence, duplicate-input and current-fee conservation checks before signatures; cached source counts. | Mempool admission is O(N × SHA3) per transaction and admits/relays transactions whose inputs do not exist; one public submission point can saturate ev… |
| NET-03 | IMPLEMENTED | 32 simultaneous inbound devnet connections per normalized IP; global cap remains 128. | Devnet inbound connection cap is global with no per-IP limit; 128 idle connections from one attacker lock every other peer out of a bootnode indefinitely |
| ST-01 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | Genesis-cohort cap hands the *non-cohort* side a calendar-fixed consensus share regardless of its stake; a single 25,000 BLCH outsider reaches 1/3 (st… |
| ST-02 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Legacy unauthenticated `Exit` (0x03) is live and un-capped today: any single proposer can force-exit the entire roster in one block, killing the chain… |
| TX-01 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Legacy `Exit` (tag 0x03) is live, unauthenticated and uncapped: one block proposer can retire the entire validator set; the survivor holds 100% of con… |
| TX-12 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Every RANDAO chain is terminal; the first exhaustions (~2027-02) start removing proposers permanently |
| BV-01 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | The deposit key and the branch-A key are the same key, so the "pre-signed U + delete the bypass key" covenant emulation is structurally impossible; a … |
| BV-02 | PARTIAL | Removed claim that RBF alone lets a keyless watchtower replace transactions; fee ladders/CPFP not implemented. | The clawback cannot be fee-bumped by a watchtower as documented; the only way to give a watchtower that power is to hand it `recovery_sk`, which lets … |
| BV-03 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `pq-shield-api` receives exactly the public keys whose secrecy the vault's security rests on (`recovery_pubkey`, `hot_pubkey`) plus the unvault intent… |
| BV-04 | PARTIAL | Explicit V3 derives hardened role paths with separate PQ domain. V1/V2 compatibility preserved; existing-funds migration remains open. | Recovery key is a non-hardened BIP-32 sibling of the hot key in *both* derivations (V1 and the A4-M-5 "fix" V2); `hot_sk` + one xpub ⇒ `recovery_sk` |
| CR-01 | PARTIAL | Duplicate input-tree positions rejected. Characterization tests demonstrate unresolved plaintext-holder authorization and cross-spend nullifier attacks; product activation blocked in authorization report. | `coherence_core::check_spend` has no spend authorization: `nk` is prover-chosen, so any note plaintext holder (incl. the sender) can spend, and one no… |
| CR-02 | PARTIAL | Signature/encoding variants share one mempool identity. Consensus Falcon canonicality still needs a coordinated gate. | Hybrid signatures are third-party malleable (SUF-CMA broken): PQClean's non-padded `falcon-1024` verifier accepts the 1280-byte zero-padded encoding; … |
| EN-05 | IMPLEMENTED | Authenticated future gossip is parked in a bounded queue until its wall slot; sync replay remains available. | Future-slot blocks are stored *and applied* immediately: a scheduled proposer can void up to 7 preceding slots, and honest clock skew voids slots by a… |
| EN-06 | PARTIAL | Proposal selection rechecks committed transfer inputs, ownership and epoch-specific fees before packing, reserves conflicting inputs, and revalidates funded deposits against one cached proposal view. Stale entries remain retryable; capacity eviction still trusts their advertised tip. | Mempool ordering and capacity eviction trust an unbacked, sender-chosen `tip_millisat_per_gas` |
| EN-07 | PARTIAL | 4,096-entry exact failed-signature cache for gossip blocks/attestations. Unique junk and per-peer verification fairness remain open. | No per-peer budget on hybrid verifications for unauthenticated attestations/transactions on the devnet mesh; rejected messages are not remembered |
| EN-08 | PARTIAL | Consensus loop handles at most 32 queued events before revisiting duties; per-peer scheduling fairness remains open. | The shared engine queue budget has no per-peer fairness: one peer can keep honest blocks and attestations shed |
| EN-09 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | A validator key can grow `blocks` (a fork-choice input) without bound above the finalized floor |
| EN-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Duties are signed against an artificially rolled stale state while the node is behind |
| EN-11 | IMPLEMENTED | Pending status uses the cached txid index. | `gettxstatus` hashes every mempool transaction on the consensus thread |
| FC-04 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | RANDAO grinding of the *next* epoch's committees and proposer schedule is free and one epoch too close (F6 open, proposer reward absent) |
| FC-05 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | LMD-GHOST has no proposer boost and a root-value tie-break: ex-ante reorgs and balancing are cheap at 2 attesters per slot |
| FC-07 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | With the 1/2 floor armed, any two disjoint sets holding ≥ 1/3 of unleaked stake each finalize conflicting checkpoints after ~25 epochs, with no slasha… |
| FC-08 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Sub-epoch duty-view lag: with `back = 1` the seed and the source checkpoint for epoch E depend on the *last block* of E−1, which honest first-slot att… |
| INF-01 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | The live validator binary is built and shipped by a pipeline that exists only outside the repository — HIGH — status: partially KNOWN (`deploy/RELEASE… |
| LG-01 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | The carried ledger holds exactly 39,917 Genesis-3 subsidies, not 39,918; the missing coinbase is unexplained and two concrete mechanisms exist in the … |
| NET-04 | PARTIAL | Repeated exact bad gossip signatures avoid hybrid re-verification. Unique inputs, logging cost and peer penalties remain open. | Unauthenticated devnet frames buy bounded but sustained consensus-thread CPU (hybrid verifies) and unbounded log spam; `Reject` has no consequence on … |
| NET-05 | IMPLEMENTED | Shared per-IP and global get-blocks budgets plus aggregate page-byte limit. | Devnet `get-blocks` serving is rate-limited per *connection* only (no per-IP/global cap), pages are 4× larger than libp2p's and carry no byte cap |
| NET-09 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Devnet idle-close at 120 s silently drops the first broadcast after an idle period; the honest cadence on non-sync connections is ~16 minutes |
| NET-10 | IMPLEMENTED | JSON value-count cap and bounded scalar IDs. | RPC JSON parser memory amplification and id echo |
| NET-11 | IMPLEMENTED | Eight RPC connections per IP; overflow sockets are dropped without blocking the accept loop. | RPC connection exhaustion/slowloris: 64 slots, 30 s deadline, no per-IP limit |
| NET-12 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Bootnode/observer hosts are a privileged frame-push position into all 63 validators |
| SR-02 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | The signer arrangement (keys + quorum rule + review clock) is not bound by the checkpoint digest |
| SR-03 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Fresh-install onboarding is currently refused: the genesis anchor aged out at epoch 2016 and no signed envelope exists anywhere in the tree |
| ST-03 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Correlated-slashing amplification mixes raw-bond penalties (numerator) with effective, capped/leaked stake (denominator): one slash of a cohort valida… |
| ST-04 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Self-slashing is a faster, cap-free exit: ejection at E+1 bypasses `MAX_EXITS_PER_EPOCH`, withdrawability lands 32 epochs *earlier* than a voluntary e… |
| ST-05 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Post-L, unauthenticated lifecycle transactions cost the node 1–4 hybrid verifications each before any per-source accounting; `tx_source_hash` returns … |
| TX-02 | PARTIAL | Transfer/funded-deposit budgets checked before execution; all classes checked after each charge. Unmetered legacy classes remain outside this fix. | Per-block byte/gas caps are enforced only after all transactions have executed; a scheduled proposer can force ~16× a maximal block's execution work p… |
| TX-06 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Zero-value and arbitrary-count outputs are valid: permanent state growth at ~6 sat per entry |
| TX-08 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Producer fee share compounds into `staked_sat` (consensus weight) with no per-block cap |
| TX-11 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Slashing is unreachable; equivocation is free |
| BV-05 | PARTIAL | API checks explicit, nested and serialized CSV delays against 144. Legacy low-level builder compatibility remains. | `csv_delay = 0` (and any tiny Δ) is accepted everywhere; branch A becomes immediately spendable and the vault silently provides no window |
| BV-06 | IMPLEMENTED | Reject empty/oversized PQ keys before the panicking wrapper. | Remote-triggerable panic in `pq-shield-api` `/anchor/commitment`: an empty or >8192-byte `pq_recovery_pubkey` reaches the *panicking* `anchor_guard_go… |
| BV-07 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | The anchor has no freshness / rotation / revocation semantics; two valid anchors for the same vault under the same trusted key are indistinguishable, … |
| BV-08 | PARTIAL | API bounds dust/value/fees and caps fees at 10 percent of input. Dynamic estimation, fee strategy and all low-level builders remain open. | No dust, absurd-fee, or fee-estimation checks in the tx builders or the API; pre-signed transactions freeze fees |
| BV-09 | PARTIAL | Owned vault PQ secret vectors clear on Drop; temporary seeds and discarded BTC secrets use zeroizing ownership. Classical Copy keys, caller copies, BIP32/third-party/OS internals remain outside guarantee. | Secret material is never zeroized: `VaultKeys` (`Clone`), `pq_secret: Vec<u8>`, `SecretKey` (`Copy`, no `Drop`), master `Xpriv`, the HKDF IKM copy |
| BV-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `r` is deterministic in `(pq_sk, vault_id)` with a client-chosen, API-invisible `vault_id`; reuse or derivation-version confusion silently degrades or… |
| BV-11 | PARTIAL | Rejects signed-anchor trailing bytes; API validates address syntax and consistent network. Low-level arbitrary anchor strings remain compatible. | `SignedAnchor::deserialize` ignores trailing bytes; anchor address fields are never validated as addresses |
| BV-12 | PARTIAL | JSON-only bounded API, cross-site checks and loopback-only binary bind. Authentication, TLS and per-client rates require deployment integration. | `pq-shield-api` deployment posture: plain HTTP, no authentication, no per-client rate limit, `0.0.0.0` bind supported; CSRF-shaped requests reach hand… |
| BV-13 | IMPLEMENTED | Anchoring HTTP bounds connect/read/write/overall time and response bytes; credentials require HTTPS, redirects refused, JSON-RPC response identity/shape checked. | `anchoring/src/http.rs`: no read/write timeout, API key sent in clear over whatever scheme the caller passes, lenient RPC parsing |
| BV-14 | PARTIAL | GitLab now runs native Ustav/EUVM and PQ boundary checks. Name-denylist limitations and full kernel/product qualification remain open. | The Ustav "PQ boundary" is a name-denylist tripwire that runs only in GitHub Actions; the GitLab pipeline neither runs it nor blocks on `bloch-ustav`/… |
| BV-15 | PARTIAL | Script evaluator rejects oversized integers, invalid CSV/conditionals/minimal-IF/stack/sighash forms. Bitcoin Core differential qualification remains open. | `script_eval.rs` diverges from Bitcoin Core in ways that matter for standardness, and it is the *only* thing validating the vault's scripts |
| CR-03 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `SUITE_MLDSA65_ONLY` (0x0002) is accepted by the live Genesis-4 transfer verifier with no activation gate, contradicting the "hybrid on every consensu… |
| CR-04 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Seed-derived keys are reused across contexts: `bloch-btc-wallet` default identity and `bloch-pq-vault` V1 derive the PQ key from the raw BIP39 seed, w… |
| CR-05 | PARTIAL | Wallet transaction construction rejects cross-network recipients in both directions. Historical checksum network binding remains unchanged for compatibility. | Address checksum does not bind the network; `Wallet::build_tx` accepts a recipient `Address` of the other network |
| CR-06 | PARTIAL | Explicit disclosure convention supports HD-v3 index zero while preserving both old derivation families and wire format. Existing consumers must adopt the correct explicit family. | Index-0 convention mismatch between `wallet::disclosure::keypair_at` and `hd_wallet::derive_at` |
| CR-07 | PARTIAL | Fallible exhausted-index import adopted by legacy CLI; pre-KDF wallet metadata rejects duplicate indices and unsupported/contradictory derived metadata while preserving imported cross-network keys. Both import entry points return Result, requiring caller handling; broader hostile-file/key authenticity audit remains incomplete. | Wallet-library robustness nits (panics / zeroization gaps on untrusted or edge inputs) |
| CR-08 | PARTIAL | Scoped seeded-RNG API adopted by production key generation; caller seed clearing and TLS teardown are covered. Forgotten standalone legacy guards and opaque ChaCha internal-state erasure remain unresolved. | `SeededRngGuard` design hazards: a forgotten guard seeds the thread forever; ChaCha state is not zeroized; `Drop` touches TLS unconditionally |
| EN-12 | PARTIAL | Held attestations use their epoch and target seed; duty index is cleaned on release. Complete branch-historical registry projection remains open. | `release_held` judges released attestations with the *wall epoch's* seed and roster, not the attestation's |
| EN-13 | IMPLEMENTED | Exact duplicate parked orphan/future blocks skip repeated hybrid verification; regression coverage. | Re-offered orphans pay a hybrid verify on every delivery |
| EN-14 | PARTIAL | Remembered refused ancestry suppresses child requests and prunes parked descendants. Unseen/evicted ancestry remains unresolved. | Orphans hanging off a latch-refused branch keep the sync pump broadcasting `get_blocks` to all peers indefinitely |
| EN-15 | IMPLEMENTED | Known-parent non-increasing slots rejected before storage insertion; slot-monotone ancestry regression. | `ancestral_boundary_mix` walk is bounded by `blocks.len()`, and stored non-canonical blocks are not slot-monotone |
| EN-16 | IMPLEMENTED | Global/unattributed proposal errors narrow the current candidate without evicting or barring innocent mempool entries. | The proposer's drop loop bars innocent transactions on non-indexed transition errors |
| EN-17 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `do_reorg` rewrites the whole block log synchronously on the consensus thread |
| EN-18 | PARTIAL | At most 16 outstanding engine RPC calls, including timed-out queued work; several read methods use SharedHead. Full RPC separation remains open. | RPC events bypass the queue budget and are answered on the consensus thread |
| EN-19 | PARTIAL | Automatic lifecycle signing now respects the doppelganger gate. No dedicated durable RANDAO-intent slashing record added. | `RandaoRecommit` is signed outside slashing protection and outside the doppelgänger gate |
| FC-06 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Node-side fork choice is order- and store-shape-dependent (O01), and feeds *every stored block* including orphans |
| FC-09 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `close_epoch` silently swallows `FinalityError::OutOfOrderEpoch`; a desynchronised engine would stop finality forever with no signal |
| FC-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Attestation and proposal signing roots bind no network/genesis identity |
| FC-11 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | The leak is absolute (satoshis) while `duty_roster_at` rescales effective stake; a residual leak can zero a validator the moment a cap binds |
| FC-12 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Committed `fc_equivocators` bar is permanent and has no exit/slash/recovery path |
| INF-03 | PARTIAL | Legacy manifest paths and introduced hardened arithmetic failures repaired. Full hosted CI and inherited formatting drift remain unqualified; GitLab fmt is informational. | GitLab's `check` stage is red on `main`, so its `test` stage never runs; the "both pipelines gate the live crates" claim is false — MEDIUM — NEW |
| INF-04 | PARTIAL | GitLab adds blocking native Ustav/EUVM and boundary checks. Complete pipeline parity and hosted execution remain open. | GitHub and GitLab pipelines diverge; the pipeline this clone actually pushes to is the weaker one — MEDIUM — NEW |
| INF-06 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | SP1 prover image: pipe-to-shell installers, floating base images, whole-repo `COPY` — MEDIUM — NEW |
| INF-07 | PARTIAL | Updated activity metric follows current registry/signing gates; corrected startup false alarms and added availability/heartbeat/store/inactivity/restart rules with target labels. Deployment, runtime rule qualification and exact missed-duty coverage remain open. | Alerting has holes and the alert docs contradict the exporter — MEDIUM — NEW |
| INF-08 | IMPLEMENTED | Host-loss and checkpoint runbooks use actual JSON-RPC/CLI interfaces and distinguish observation from old-host fencing. | Runbooks name a CLI and an RPC method that do not exist, on the host-loss fencing path — MEDIUM — NEW |
| INF-09 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | No rollback is currently possible, and the release signing flow is unspecified — MEDIUM — KNOWN (`deploy/RELEASE-INTEGRITY.md` §8.7), restated because… |
| INF-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Bootnodes expose the full unauthenticated PoS JSON-RPC (incl. `sendrawtransaction`) on `:8080`, contradicting the published posture — MEDIUM — status:… |
| INF-11 | PARTIAL | Guard/scorer regression coverage rejects additional nonexecuted, waived and malformed runs. No claim to prove arbitrary shell/YAML workflow semantics. | The `check-*-blocking` guards are bypassable by trivial spellings and cover only five jobs — LOW — NEW |
| INF-12 | IMPLEMENTED | Installer requires committed platform/version SHA256 pins, verifies before extraction and atomically replaces cached binaries. CI executes the verified absolute path. Offline bypass regressions pass; pins originate from upstream HTTPS, not independent signatures. | Scanner binaries are pinned by version, not by hash; a pre-existing binary on the self-hosted runner is trusted blindly — LOW — NEW |
| INF-13 | IMPLEMENTED | Docker context exclusions for common key/passphrase files; existing history/images are not cleaned. | `.dockerignore` does not exclude key material; only `COPY . .` makes it bite — LOW — NEW |
| INF-14 | PARTIAL | Actual scalar must carry exact digest; comment exemptions removed, non-pullable template sentinel and one same-service build/never-pull fixture allowed. Expanded bypass tests pass; arbitrary YAML inheritance remains outside structural guard. | Image-pin guard exemptions are loose and its own docs are stale — LOW — NEW |
| INF-15 | PARTIAL | Retired local compose RPC mappings bind loopback. Explorer/pool proxy now requires configured HTTPS without public wildcard fallback, bounds bytes/time and refuses redirects. Live TLS, DNS and origin firewall qualification remain pending. | Retired-but-deployable configs publish unauthenticated RPC; explorer upstream is plain HTTP via a third-party DNS — LOW — NEW (explorer part is KNOWN … |
| INF-16 | IMPLEMENTED | Rehearsal uses repository-owned serialization wrapper and refuses a missing wrapper; owner/mode checked cooperative lock replaces world-writable temporary executable. | `scripts/prova-relanca.sh` executes a wrapper from world-writable `/private/tmp` — LOW — NEW |
| INF-17 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | The attested/appliance image keeps sshd enabled with NixOS defaults; the persist-volume encryption design is internally inconsistent — LOW — NEW |
| INF-18 | PARTIAL | PoS module requires explicit package rather than falling back to legacy PoW binary; mesh transport wiring added. Other Nix modules and real NixOS evaluation remain open. | Nix modules that cannot work as shipped — LOW — mostly KNOWN (admitted TODOs) |
| INF-19 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | History claims (`catalog-dev.secret.pem`, "55 findings", leaked PAT) cannot be verified from this clone; CI never scans history — LOW — status: KNOWN … |
| INF-20 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Minor key-handling and hygiene items — LOW — NEW |
| KS-01 | IMPLEMENTED | Key generation takes the directory lock and refuses existing validator.key. | `keygen` silently overwrites an existing `validator.key` (no `create_new`, no lock, no confirmation) |
| KS-02 | IMPLEMENTED | Production passphrase-file input uses the same permission checks as sealing. | Production passphrase file (`BLOCH_KEYSTORE_PASSPHRASE_FILE`) is not mode-checked; only the `keys seal --passphrase-file` path is |
| KS-03 | IMPLEMENTED | All new sealed writes require 12 characters; this does not strengthen existing passphrases. | No minimum passphrase length on the `keygen` / env path; the mainnet ceremony sealed 64 keystores through it |
| KS-04 | PARTIAL | Offline bound export/import and minimum-slot CLI implemented; monotone merge and atomic private writes tested. External old-host fencing and complete prior-signature bounds remain operational prerequisites. | Slashing protection has no import/export and no "minimum slot" initialization; the host-loss runbook's fencing step cannot be executed with the shippe… |
| KS-05 | PARTIAL | Private unbuffered terminal reads, preallocated zeroizing buffers, checked RAII restoration and echo disabled before prompt. Ceremony signal traps tested; native node terminating-signal restoration remains open. | Interactive passphrase entry: echo not restored on signal, `tcsetattr` restore result ignored, and `Stdin`'s buffer retains the passphrase |
| KS-06 | IMPLEMENTED | Secret hex buffers zeroized, signing key file mode checked, new secret file and directory fsynced. | `ws-sign` / `ws-signer-set` key-file hygiene: non-zeroized hex copy of the secret, no mode check on `.sk`, `.sk` write not fsynced |
| KS-07 | PARTIAL | Read-only block-log-inspect reports bounded framing/decode diagnostics without modifying files or invoking repair. Per-frame checksums and replay-transition recovery policy remain open. | Block log has no per-frame checksum; a zero-filled tail is classified as corruption (boot refusal, no repair tool); a replayed block that fails `apply… |
| KS-08 | IMPLEMENTED | Key mutations verify ownership/type before writes/locks; reject symlinks. Read-only inspect no longer creates LOCK. | `keys seal` / `keys inspect` run as another user leave root-owned `validator.key` / `LOCK`, so the next node start fails |
| KS-09 | PARTIAL | Source digest includes .macros, hidden source directories and root toolchain. Complete build environment provenance remains open. | Source-digest scope gaps: a compiled C include (`.macros`) and dot-directories are outside the hash; toolchain env not captured |
| KS-10 | IMPLEMENTED | Atomic private replacement plus fsync for validator.key and initial meta.bin. | Non-atomic writes: `save_with` truncates `validator.key` in place; `meta.bin` written with `fs::write` and no fsync |
| KS-11 | PARTIAL | Default combined Argon2 memory/pass work capped at 1 GiB-pass before allocation; production parameters unchanged. Explicit legacy recovery remains bounded by original individual maxima and cannot authorize expensive new seals. A one-pass header can still allocate 1 GiB. | Header-supplied KDF cost is honored down to the Argon2 floor with no minimum or warning; caps still allow minutes of CPU per open |
| KS-12 | IMPLEMENTED | Root toolchain pinned to Rust 1.94.1. | Toolchain pin is crate-scoped; documented root-level build commands bypass it |
| LG-02 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | vout endianness (Legacy M-3): the bug is in the exporter/`iter_utxos_sorted`, the Genesis-4 loader carries the corrupted index verbatim into committed… |
| LG-03 | IMPLEMENTED | UTXO iterator/export fail on malformed keys, undecodable values or trailing bytes; valid historical endian behavior preserved and tested. | Snapshot exporter and `iter_utxos_sorted` silently drop undecodable UTXO rows (fail-open on the ledger-producing path) |
| LG-04 | IMPLEMENTED | Canonical address before cooldown lookup. | Faucet per-address cooldown is bypassable by hex case variation |
| LG-05 | IMPLEMENTED | Require application/json and reject cross-site Fetch Metadata. | Faucet accepts cross-site form POSTs (CSRF-driven drips) |
| LG-06 | PARTIAL | Shared explorer/pool transport enforces HTTPS configuration, read allowlists, bounded requests/responses and deadlines. No plaintext wildcard fallback. Direct archival RPC exposure, origin read-only policy and live deployment remain unverified. | Explorer/pool-site RPC path: plaintext upstream via a third-party wildcard DNS, and the archival node's full unauthenticated RPC (write methods includ… |
| LG-07 | PARTIAL | Bounded staged sync checks linkage, anchor/tip observations, serializes concurrent passes, and rejects malformed RPC hashes/transaction fields before publication. Legacy height index does not prove canonical chain membership; total sync deadline, whole-state rewrites, pruning and existing-data reconciliation remain open. | Reference indexer: unbounded responses and whole-state rewrite |
| LG-08 | IMPLEMENTED | Reject non-ASCII/oversized satoshi strings and empty display values; generator updated too. | Python SDK amount parsing accepts non-ASCII digits and has an uncaught-exception path |
| NET-06 | IMPLEMENTED | Read at most 14 request bytes for the 13-byte libp2p sync message. | libp2p sync codec buffers up to 8 MiB per inbound *request* substream for a 13-byte message |
| NET-07 | PARTIAL | One existing libp2p sync fanout slot rotates independently of claimed height. Unvalidated hints, Sybil churn and total outstanding-request control remain open. | libp2p sync steering: `peer_head` is set from the unvalidated header slot before the engine judges, and the top-`SYNC_FANOUT` claimants are always chosen |
| NET-08 | IMPLEMENTED | Identify advertisements cannot overwrite configured dial ownership. | libp2p: identify-advertised addresses overwrite the `dialed` map, letting a connected peer suppress redials to honest configured peers (eclipse assist) |
| NET-13 | IMPLEMENTED | Failure to install peer scoring is fatal at transport initialization. | libp2p `with_peer_score` failure is warn-not-fatal; a scoring-less node is a flood amplifier |
| NET-14 | IMPLEMENTED | Connection-lifetime slots replaced by finite FIFO request leases shared across timer/engine, with expired-queue authorization checks and backpressure resumption. Silent-peer/inbound-response regressions pass. Response completion, Sybil resistance and fleet recovery SLA remain separate limitations. | Devnet sync slots are sticky for the connection lifetime; a silent slot-holder throttles catch-up |
| NET-15 | PARTIAL | Engine and periodic sync share two request leases; reverse-direction serving has per-connection and shared worker caps across reconnects. Legacy wire lacks response IDs/end markers, so exact outstanding-response bounds remain unimplemented. | The engine's sync pump broadcasts `get-blocks` to every devnet peer; each answers a 512-block page |
| NET-16 | IMPLEMENTED | Whole-request metrics deadline. | Metrics server has no whole-request deadline (per-read timeout renews) |
| NET-17 | IMPLEMENTED | 8 MiB encoded page budget; first frame allowed for progress, frame cap still applies. | Devnet page has a block-count cap but no byte cap (unlike libp2p) |
| NET-19 | IMPLEMENTED | Private atomic fsynced identity-file creation under the existing node directory lock. | libp2p identity file is written non-atomically with the default umask before `chmod 0600`, and the chmod result is ignored |
| NET-20 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Frame-cliff vs consensus caps on the production transport |
| SR-01 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Genesis cohort is bound by neither the state root nor the genesis block id (only by the 32-bit `network_id`) |
| SR-04 | IMPLEMENTED | ws-verify enforces the boot arrangement window and refuses duplicate keys. | `ws-verify` diverges from the booting node: it omits the `arrangement_window` lower bound and carries stale duplicate-key text |
| SR-05 | PARTIAL | Stored/supplied checkpoint state roots are checked against locally validated canonical block evidence; genesis conventions preserved. Missing local evidence warns. Validator-set roots and post-sync cross-check lifecycle remain incomplete. | A checkpoint's `state_root` / `validator_set_root` are never validated against the block they name, on either side |
| SR-06 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `single_derivation_path` has scan blind spots (currently clean) |
| SR-07 | IMPLEMENTED | Standalone verifier rejects zero/impossible thresholds and duplicate signer public keys; node and committee regressions. | `ws::verify_envelope` alone accepts a zero-threshold arrangement and does not compare signer keys; the crate relies on the node decoder for both |
| SR-08 | IMPLEMENTED | Decoder bounds counts by remaining bytes and shared attestation cap before allocation. | `codec::decode_envelope` pre-allocates from untrusted counts and hard-codes the attestation cap |
| SR-09 | IMPLEMENTED | Three ADR-041 commitment regressions pin existing encodings/zero semantics, mutate every key/value and test unique-entry order independence. Existing tag registry extended through 0x1E; production roots unchanged. | ADR-041 leaves (`0x1B`–`0x1E`) have no root-binding test, and the spec registry stops at `0x16` |
| ST-06 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `close_epoch` mints delegator issuance shares into the ledger without advancing `issued_sat`; the supply-conservation rule would then refuse every bou… |
| ST-07 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Attestation and proposal signing roots carry no network/genesis binding, so a validator key reused on another network (devnet, a fork with the same sl… |
| ST-08 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | Cohort-cap `Deferred` threshold is a cliff: one 5% slash, an exit, or a leak-independent effective-stake dip of the sole independent validator below 2… |
| ST-09 | PARTIAL | Same admission-total correction as TX-05. | Mempool and consensus derive the funded-deposit stake cap from different totals (unleaked vs leak-applied roster) |
| TX-03 | PARTIAL | Mempool deduplicates semantic txid across encodings. Consensus bytes and historical identifiers unchanged. | Single-input transfers are cross-format malleable: a relay can re-encode V1↔V2 producing a distinct, valid `canonical_bytes` with the same `txid` |
| TX-04 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `network_binding()` is a compile-time label, not a genesis-derived value, so the (inert) sighash binding does not actually separate networks built fro… |
| TX-05 | PARTIAL | Lifecycle admission and revalidation now use leak-adjusted active roster like current-state transition. Next-epoch changes still revalidate on-chain. | Mempool and consensus compute the funded-deposit cap from different `total_active` values (leak-applied vs unleaked) |
| TX-09 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Issuance accounting below `REWARDS_V2`: delegators receive zero issuance, leak does not reduce income, credit is one unscoped bit, withheld proposals … |
| TX-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Staking-class transactions are unmetered (0 gas, 0 bytes) and there is no consensus transaction-count cap |
| TX-13 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `MAX_EPOCH_ADVANCE = 4096` is a hard liveness ceiling: a network-wide stall longer than ~45.5 days can only be resumed by a hard fork |
| TX-14 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Genesis bonded 1,600,000 BLOCH outside `issued_sat`; the conservation invariant is a one-sided delta that can never see it, and burns are uncounted |
| TX-15 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Duplicate `(validator, signing_root)` attestations cost one hybrid verify each and are accepted (idempotent) |
| TX-16 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Body decode, re-encode and Merkle hashing (steps 3b) run before the proposer signature (step 7); an unauthenticated peer can force ~8 MiB of hashing p… |
| BV-16 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Chameleon: no chameleon hash / trapdoor exists; the actual "forgery capability" belongs to whoever supplies `TrustedBurnCheckpoint`, and it is total (… |
| BV-17 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `hybrid_wbtc_validator` (the "Custody 2-of-2" anchor guard) is never executed in a test, and it cannot run on the PQ-only Ustav kernel the anchor is s… |
| BV-18 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Documentation overclaims relative to the code (collected) |
| BV-19 | PARTIAL | Fallible explicitly versioned V1/V2/V3 restore preserves existing outputs. Old V1 convenience API retains historical panic contract; consumers must use safe API for untrusted seed input. | Minor robustness items in `bloch-btc-wallet` / `bloch-pq-vault` derivation |
| BV-20 | PARTIAL | Unknown/conflicting fields rejected; malformed-field errors no longer echo raw input and verify endpoint omits unnecessary copied public-key/commitment fields. Required public construction artifacts remain; arbitrary secret content cannot be detected. | `guard_no_secrets` inspects key names only; a secret placed in a free-form value passes and is echoed back; `deny_unknown_fields` is ineffective on th… |
| BV-21 | PARTIAL | Carrier ambiguity and nonzero padding rejected. Signer binding and consensus transaction codec remain open. | `anchoring/` convention: first-`BLA1`-output heuristic, no signer binding, non-consensus tx codec |
| CR-09 | IMPLEMENTED | Fork README, NOTICE and VENDOR documentation now describe the actual build/dependency changes. Documentation correction only; authenticated upstream equivalence is not established. | Fork provenance documentation is inaccurate: README/NOTICE/VENDOR.toml claim only `src/lib.rs` changed and `build.rs` is identical to upstream; `build… |
| CR-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Legacy raw-signature magic ambiguity (1/65536) remains for signatures |
| CR-11 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | No standards-traceable KATs for ML-DSA-65 / Falcon-1024; seeded golden vectors pin the *crate*, not the *standard* |
| CR-12 | PARTIAL | New checked ASERT API rejects regressed heights, overflow and malformed/zero targets; unit/property/no_std checks pass. Historical consensus callers retain old behavior pending migration. | Genesis-3 PoW crate: `asert_next_bits` underflows on `new_height < anchor_height`; `bits_to_target` maps invalid compact bits to `Target::MIN` |
| EN-20 | IMPLEMENTED | One registry-identity helper serves production and tests, preserving consumed-reveal and generation-specific RANDAO checks. Nine registry gate tests and integrated node suite cover it. | Two implementations of the boot identity rule; the tested one has no production caller |
| EN-21 | IMPLEMENTED | Actual genesis validator index membership replaces dense-index assumption; sparse genesis identity and deposited-key retry regressions pass. | `genesis_validator_count` assumes dense manifest indices |
| EN-22 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Env/flag-driven node-local safety behaviour (summary; see §3) |
| EN-23 | PARTIAL | Base includes persisted, validated restart caches and incremental state roots; cold full replay and historical retention still need production-scale qualification. | Per-ingest cost and memory grow with chain age; replay is quadratic |
| EN-24 | IMPLEMENTED | Same shared sync budget as NET-05. | Serving `get_blocks` is rate-limited per connection, not per peer/IP |
| FC-13 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Arithmetic/panic notes (Info) |
| FC-14 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Documentation / spec divergences (Info) |
| INF-05 | REFUTED IN AUDIT | Auditor explicitly refuted bans failure; do not count as a fix. | `deny.toml` duplicate allowlist no longer matches `Cargo.lock`; the blocking `supply-chain` gate is very likely red — MEDIUM — NEW |
| INF-21 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | A checked-in agent workflow with a stale, founder-specific context — INFO — NEW |
| INF-22 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Documentation rot that would mislead an operator — INFO — NEW |
| KS-13 | IMPLEMENTED | Keystore runbook now describes the supported explicit sealing command. | Stale documentation: `KEYSTORE-AT-REST.md` says "No re-seal tool" and "Option A is the only one an operator can actually execute" |
| KS-14 | IMPLEMENTED | Ceremony uses fresh inherited pipes rather than secret environment values; bounded/deadlined reader, closed descriptor, preallocated zeroizing buffers, disabled xtrace/allexport and PTY regressions. Legacy secret-environment API remains compatible outside this ceremony. | Ceremony script exports the passphrase into the environment of 64 child processes |
| KS-15 | PARTIAL | Drill creates a new private directory and unlinks only its four disposable secret files on normal/error/signal exits. SIGKILL/power loss and secure media erasure cannot be covered by shell cleanup. | `scripts/ws-ceremony-drill.sh` leaves throwaway signer `.sk` files in its work dir |
| KS-16 | PARTIAL | WS policy and malformed/configuration artifact refusals are typed exit 78; raw filesystem I/O remains ordinary failure. Supplied units suppress 78 and rate-limit other restarts; fleet deployment remains unverified. | WS boot refusal + `Restart=on-failure` is a replay-every-5-seconds crash loop |
| KS-17 | IMPLEMENTED | Plaintext opt-in consumed only as a recognized run/keygen option, never as another option value; regression test. | `--allow-plaintext-keystore` is matched anywhere in argv |
| KS-18 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Doppelganger protection is in-memory, window-bounded and flag-bypassable |
| LG-09 | PARTIAL | New authorized pool sessions use canonical parsed address spelling; proof-to-accounting regression joins case aliases. Historical journals/credits remain unchanged and need reconciliation before manual payouts. | Pool: shares and ledger keyed by the raw `mining.authorize` username, not the parsed address |
| LG-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Documentation drift on the ledger-critical code (dust recipient, snapshot vintage, zero-value rows, dead "tip height" print) |
| LG-11 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Pool/pool-proxy: advisor findings verified closed; residual notes for a redeploy |
| LG-12 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | euvm / ffg reachability from the live node |
| LG-13 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | coherence-prover / SP1, spikes, fuzz |
| NET-21 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Information exposure by design (Info) |
| NET-22 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Minor code-level notes (Info) |
| SR-10 | IMPLEMENTED | State-root documentation now accurately describes bounded thread-local RefCell memoization and its non-authoritative role; no production behavior changed. | `state_root.rs` doc claims "no global mutable state" while holding a thread-local two-generation memo (consensus-safe, doc false) |
| SR-11 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Domain-tag hygiene: several tags cover two or three preimage shapes, docs are stale, and three tags live outside the registry |
| SR-12 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | The weak-subjectivity window's slashability premise is void while slashing, exits and withdrawals are unarmed |
| SR-13 | PARTIAL | Same-prefix issuance remains immutable. Optional owned0700 publication directory coordinates network/genesis/epoch records across prefixes using fsynced atomic no-replace publication, including races. Independent registries, old tooling and omitted option remain outside protection. | Same-epoch re-mint is a permanent boot refusal for every node that stored the first artifact |
| ST-10 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Stale/contradictory comments on the slashing path |
| ST-11 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `genesis_principal_sat` and `admission_network_domain` are uncommitted, manifest-derived consensus inputs; the domain is `SHA3(manifest.encode())`, so… |
| ST-12 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Arming L also switches on gas/byte metering for ExitV2, RandaoRecommit and SlashingEvidence (`staking_tx_charge` reads `\ |
| ST-13 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Activation queue: intra-epoch order is grindable by `pubkey_hash` and the queue is unbounded (economically bounded by 25,000 BLCH per entry locked ≥ 2… |
| ST-14 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Mutation guard (`scripts/check-validator-lifecycle-mutations.py`) matches the code and each of its eight mutations is killed by `transition::tests::va… |
| ST-15 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Whistleblower is always the *including proposer*; the observer who gossips evidence earns nothing, and evidence is trivially front-run |
| ST-16 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | A queued funded validator whose deposit epoch is never finalized has no exit: `ExitV2` requires `activation_epoch <= epoch`, `Withdraw` requires an ex… |
| TX-17 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `FundedDeposit::decode` enforces consensus rules (`validate_shape`) inside the decoder, contrary to the stated decode/judge split |
| TX-18 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `Delegate.eligible` is a wire-supplied bit that consensus records verbatim |
| TX-19 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Spec/code drift in `BLOCH-L1-FEE-MARKET.md` and `BLOCH-TOKENOMICS-V4.md` |
| TX-20 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | `interfaces.rs` `StateRoots` (14 fields, "closed again at eight components") vs `state_root.rs` (~25 tags) and the frozen `StateTransition::apply_bloc… |
| TX-21 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Two block-validation stacks (`derive::validate_block` vs `transition`) still coexist |
| TX-22 | PARTIAL | New genesis/genesis-mainnet CLI creation rejects duplicate validator indices/public keys and inconsistent cohorts before output. Historical decoding and committed-state constructors remain compatible; arbitrary programmatic callers still require separate policy. | `CommittedState::genesis` silently overwrites duplicate validator indices / pubkeys from the manifest |
| NET-18 | PARTIAL | Nix module requires explicit transport matching peers, wires devnet mesh options and validates dual ports. Wrong default/comment removed; NixOS evaluation/deployment remains unverified. | Deploy/config drift: NixOS PoS module defaults to `--transport libp2p`, which does not interoperate with the live fleet; its comment about the binary'… |
| TX-07 | OPEN | Not closed by this bundle. Requires dedicated implementation, protocol design, external operational evidence or product-owner integration; original finding remains tracked. | Declared `tx_bytes` may exceed the encoding by any amount; one small transfer can fill the block byte budget |
| LD-01 | IMPLEMENTED | Root rustls 0.23.45 and rustls-webpki 0.103.15; configured cargo-audit passes. | Rustls advisory |
| LD-02 | PARTIAL | Updated inaccurate bincode rationale; cross-workspace exception pruning and stale-lock cleanup deferred pending all-lockfile verification. | Lockfile and advisory exception hygiene |
| LD-03 | OPEN | Legacy PoW test optimization not part of the live-node security patch. | Unoptimized legacy PoW test duration |
