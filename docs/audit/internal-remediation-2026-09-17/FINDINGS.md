# Internal audit remediation ledger — 2026-09-17

Audited source: `562e220`. Remediation base: `b066e3c`. Branch: `fix/internal-audit-20260917`.

All 200 findings are retained, including duplicates. IMPLEMENTED means code changed in this branch, not deployed, externally audited, or universally resolved on the live fleet. PARTIAL is not closure. No production binary was published.

IMPLEMENTED: 71, PARTIAL: 98, UNARMED CANDIDATE: 15, PROTOCOL DECISION: 5, BASE CHANGED: 7, OPEN: 1, REFUTED IN AUDIT: 1, VERIFIED POSITIVE: 2.

| Finding | Status | Evidence / remaining work | Original title |
|---|---|---|---|
| EN-01 | IMPLEMENTED | Oversized admission refusal plus continue-on-nonfitting selection; regression tests. | One admissible transaction censors every transaction on the network (`select_transactions` breaks on the first over-cap entry) |
| EN-02 | IMPLEMENTED | 16 MiB encoded-payload budget in addition to count; cached byte accounting. RSS includes extra object overhead. | Mempool is bounded by count (4,096) but not by bytes: ~28 GB per node from one unauthenticated peer |
| EN-03 | PARTIAL | Base already filters pre-start duties; held-attestation release now runs the same doppelganger check. Not a durable anti-cloning mechanism. | Doppelgänger protection halts a validator permanently when its *own* pre-restart attestation is replayed to it |
| EN-04 | IMPLEMENTED | Cached source counts and transaction identity index replace repeated mempool hashing. | `on_transaction` runs an O(mempool) SHA3 scan *before* any cheap refusal: ~40 ms of consensus-thread CPU per 120-byte frame |
| FC-01 | UNARMED CANDIDATE | All-zero consensus-roster fallback and regression exist behind u64::MAX. Not a live fix; needs end-to-end finality/partition/reward qualification and coordinated activation. | Post-e1400, ~60 epochs without any included attestation leaks every validator to zero and the chain dies permanently (no proposer can ever be drawn) |
| FC-02 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | Genesis-cohort cap hands a supermajority to the first minimum-bond independent validator once the taper is deep |
| FC-03 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Unauthenticated legacy `Exit` (tag 0x03) is consensus-valid: one scheduled proposer can retire the other 63 validators and own the roster after 32 epochs |
| INF-02 | PARTIAL | Deep bootnode verification no longer accepts the fleet administrator credential as a fallback. It requires `BLOCH_VERIFY_RO_KEY`, sends only a fixed `verify` token, and now ships the root-owned ForceCommand wrapper that accepts exactly that token and performs only the three documented read-only checks. Self-tests prove the admin-only environment fails and the dedicated path succeeds. No host key was rotated and the validator-management scripts still require independent per-host/operator migration, so the fleet-wide credential finding is not closed. See WAVE-48-OPERATIONS.md. | One credential class controls the majority of validator keys — HIGH — status: KNOWN (`deploy/SSH-ROLE-SEPARATION.md`, FLAG-DAY-EPOCH-800 "Known gaps")… |
| NET-01 | PARTIAL | RPC per-IP/queue bounds retained. All eUTXO and validator-registry-only reads now use the identical immutable `Arc<CommittedState>` published by the consensus owner; validator lookup/count/list no longer queue behind duties. For retained non-genesis blocks, `getblockbyslot` and `getblockbyid` now borrow immutable envelopes directly from the authoritative block map instead of cloning proportional transaction, attestation and signature allocations solely to render header fields and body counts; genesis remains a small synthesized owned envelope. Header hashing and JSON construction, all other block/store work, mempool, transaction-status and submission methods still run on the consensus thread, and RPC authentication, production exposure and fleet qualification remain unchanged. See WAVE-52-NET01.md and WAVE-178-NET01-BORROWED-BLOCK-RPC-LOOKUP.md. | Public bootnodes expose the full unauthenticated RPC (incl. `sendrawtransaction`) on `:8080`, and RPC executes on the consensus thread |
| NET-02 | IMPLEMENTED | Ownership, existence, duplicate-input and current-fee conservation checks before signatures; cached source counts. | Mempool admission is O(N × SHA3) per transaction and admits/relays transactions whose inputs do not exist; one public submission point can saturate ev… |
| NET-03 | IMPLEMENTED | 32 simultaneous inbound devnet connections per normalized IP; global cap remains 128. | Devnet inbound connection cap is global with no per-IP limit; 128 idle connections from one attacker lock every other peer out of a bootnode indefinitely |
| ST-01 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | Genesis-cohort cap hands the *non-cohort* side a calendar-fixed consensus share regardless of its stake; a single 25,000 BLCH outsider reaches 1/3 (st… |
| ST-02 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Legacy unauthenticated `Exit` (0x03) is live and un-capped today: any single proposer can force-exit the entire roster in one block, killing the chain… |
| TX-01 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Legacy `Exit` (tag 0x03) is live, unauthenticated and uncapped: one block proposer can retire the entire validator set; the survivor holds 100% of con… |
| TX-12 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Every RANDAO chain is terminal; the first exhaustions (~2027-02) start removing proposers permanently |
| BV-01 | PARTIAL | Experimental opt-in separated-deposit construction requires distinct compressed role keys for new outputs and rejects tested hot-key deposit bypass. Existing funded constructors/service remain unchanged; independent key generation/deletion, covenant enforcement and quantum race are not solved. | The deposit key and the branch-A key are the same key, so the "pre-signed U + delete the bypass key" covenant emulation is structurally impossible; a … |
| BV-02 | PARTIAL | A bounded checked builder and non-custodial API route now produce 2–32 strictly increasing clawback replacements with distinct `SIGHASH_ALL` values for offline pre-signing. A keyless watchtower can receive those finite signed alternatives without `recovery_sk`. Package delivery/refresh, dynamic fee selection, BIP-125 acceptance evidence and CPFP remain external or unimplemented. See WAVE-49-VAULT-FEE-LADDER.md. | The clawback cannot be fee-bumped by a watchtower as documented; the only way to give a watchtower that power is to hand it `recovery_sk`, which lets … |
| BV-03 | PARTIAL | The service now refuses non-loopback listeners and documents local construction as the preferred privacy boundary; any exposure requires a local TLS/auth/rate-limited proxy. It explicitly warns that public keys and unvault plans remain threat-model-sensitive. A remote operator or its proxy can still observe submitted public inputs, so this is not closed. See WAVE-36.md. | `pq-shield-api` receives exactly the public keys whose secrecy the vault's security rests on (`recovery_pubkey`, `hot_pubkey`) plus the unvault intent… |
| BV-04 | PARTIAL | Explicit V3 derives hardened role paths with separate PQ domain. V1/V2 compatibility preserved; existing-funds migration remains open. | Recovery key is a non-hardened BIP-32 sibling of the hot key in *both* derivations (V1 and the A4-M-5 "fix" V2); `hot_sk` + one xpub ⇒ `recovery_sk` |
| CR-01 | PARTIAL | Duplicate input-tree positions rejected. Characterization tests demonstrate unresolved plaintext-holder authorization and cross-spend nullifier attacks; product activation blocked in authorization report. | `coherence_core::check_spend` has no spend authorization: `nk` is prover-chosen, so any note plaintext holder (incl. the sender) can spend, and one no… |
| CR-02 | PARTIAL | Signature/encoding variants share one mempool identity. Strict APIs cover Falcon, suite envelopes, trusted-metadata legacy objects, vault/disclosure/Ustav boundaries, pool authorization, wallet messages, the public wallet keypair verifier, deposit/funding checks, funding-plan autocheck, payout inspection and deposit inspection. Compatible product paths try expected envelope and raw representations before final historical mixed-format fallback. Broader product adoption, custody-artifact migration, mixed-version behavior and a coordinated consensus gate remain open. See WAVE-57-CR02.md, WAVE-58-CR02.md, WAVE-59-CR02-RAW-CANONICAL.md, WAVE-60-CR02-VAULT-CANONICAL.md, WAVE-61-CR02-DISCLOSURE-CANONICAL.md, WAVE-62-CR02-CLI-CANONICAL.md, WAVE-63-CR02-USTAV-CANONICAL.md, WAVE-65-CR10-POOL-AUTH.md, WAVE-66-CR10-WALLET-MESSAGE.md, WAVE-67-CR10-DEPOSIT-FUNDING.md, WAVE-68-CR10-FUNDING-VERIFY.md, WAVE-69-CR10-PAYOUT-INSPECT.md, WAVE-70-CR10-DEPOSIT-INSPECT.md, WAVE-71-CR10-NATIVE-FUNDING.md and WAVE-72-CR10-KEYPAIR-VERIFY.md. | Hybrid signatures are third-party malleable (SUF-CMA broken): PQClean's non-padded `falcon-1024` verifier accepts the 1280-byte zero-padded encoding; … |
| EN-05 | IMPLEMENTED | Authenticated future gossip is parked in a bounded queue until its wall slot; sync replay remains available. | Future-slot blocks are stored *and applied* immediately: a scheduled proposer can void up to 7 preceding slots, and honest clock skew voids slots by a… |
| EN-06 | PARTIAL | Proposal and capacity selection recheck current backing; stale entries cannot retain capacity solely through advertised tips. Eviction is planned before signature checks and committed only for valid incoming transactions; funded epoch2884 regression passes. Broader ordinary-transfer replacement/package policy remains separate. | Mempool ordering and capacity eviction trust an unbacked, sender-chosen `tip_millisat_per_gas` |
| EN-07 | PARTIAL | Bounded exact failed-signature caching covers block/attestation gossip and transfer/funded/lifecycle admission. Transported blocks, attestations, transactions and HTTP RPC submissions share a 1,024-call wall-slot ceiling and a 128-call share per normalized source IP or authenticated libp2p PeerId; identity survives transport queues and block promotion. Cached failures cost none, and exhaustion is ignored/retryable rather than peer guilt. Embedded/source-free RPC remains aggregate-only; lifecycle has its separate ceiling, and Sybil/penalty policy remains open. See WAVE-60-NET04-VERIFICATION-BUDGET.md, WAVE-61-NET04-SOURCE-FAIRNESS.md, WAVE-62-NET04-BLOCK-SOURCE-FAIRNESS.md and WAVE-63-NET04-RPC-SOURCE-FAIRNESS.md. | No per-peer budget on hybrid verifications for unauthenticated attestations/transactions on the devnet mesh; rejected messages are not remembered |
| EN-08 | PARTIAL | Both transports reserve bounded per-source event/byte capacity before the first engine-facing channel; exact canonical wire charges survive later queue accounting, cleanup and release. Block queue charging now uses the shared exact encoded-length authority instead of allocating and copying a complete second envelope solely to obtain its size; the field scan and exact raw/canonical length comparison remain. Attestation queue charging likewise shares one exact fixed-fields-plus-signature-length authority with envelope accounting, eliminating the proportional temporary signature copy while preserving the exact raw/canonical length comparison. Admitted work uses bounded cost-aware class slices, while future blocks, waiting/ready orphans, finality-refused IDs and pending attestations retain their global/source count and byte caps. Orphan byte-cap eviction now sums waiting and deferred retained-byte metadata once per admission and subtracts each FIFO-evicted entry's cached exact charge, removing repeated full-queue sums without changing caps, order, eviction metrics or recovery behavior. Finality-refusal cleanup now builds one bounded parent-to-child index for waiting orphans and removes the discovered descendant set with one order-preserving retain, eliminating repeated FIFO searches/removals while preserving survivor tuples, counters, `needs_sync`, verdicts and recovery behavior. Finality-refusal FIFO membership now uses one temporary bounded ordered identity index synchronized with every FIFO pop and append, replacing one full parked-ID scan per refused branch envelope while preserving sequential reappearance, exact FIFO order/dedup/cap and the existing admission-door/recovery behavior. Deep canonical replay outside the bounded snapshot window now borrows each retained envelope directly from the authoritative block map instead of cloning the whole prefix aggregate; the required per-block proposal header/signature clones, decoded transactions, state allocations and replay complexity remain. Locally produced blocks borrow the stored envelope into one private prepared frame/id pair, removing one body clone and one libp2p decode without changing frames, topics, caps, ordering, verdicts or recovery. Devnet writer queues share each immutable frame through private `Arc<[u8]>` ownership; socket writes stream the length prefix and payload, and sync block replies additionally stream their tag and stored envelope without constructing concatenated frame buffers. Directed libp2p sync preflights each canonical fixed header against its exact framing-aware page budget before reading or allocating the body, so an exact-fit prefix is unchanged and only the first rejected boundary body is skipped; it then streams the unchanged response header and returned owned envelopes without a second aggregate encoding. The live libp2p sync response reader parses fixed headers and bodies directly into the final per-envelope vectors, removing the raw whole-response aggregate and body recopy; locally produced block framing likewise writes the canonical envelope directly into its final tagged frame, live fixed-size sync requests use stack arrays in both directions, and the devnet transaction injector streams the typed prefix, tag and borrowed payload without a proportional aggregate. Typed local libp2p block broadcasts now move their canonical payload allocation directly into gossipsub after duplicate suppression; dual transport retains only the separate tagged owner required by devnet. Typed admitted-transaction broadcasts likewise move the untagged canonical allocation directly into the libp2p transaction topic, while devnet preserves `FRAME_TX || payload` and dual transport retains only its necessary tagged owner. `sendrawtransaction` now derives one canonical byte owner and moves the decoded transaction through a private prepared-result seam on successful or duplicate admission, eliminating the former full transaction clone and second response encoding while preserving admission order, mempool/transport bytes and receipt JSON; refused paths do not gain the response SHA3. The public/devnet store page semantics remain unchanged, while a historical first oversized directed-sync envelope still yields the same empty page and periodic same-cursor recovery livelock as before. FIFO, queue depth/drop/reconnect, sync authorization, exact wire bytes and partial-write failure behavior remain unchanged. `PosTransaction` queue sizing still materializes its full canonical bytes for transported raw/canonical parity and source-free charging; no allocation-free exact authority exists without refactoring all consensus codec variants. RPC hex decoding, final decoded transaction-field ownership, one canonical encoding and the successful-receipt SHA3 remain. Canonical transaction admission now derives the witness-free transaction identity once and reuses it across the recent-inclusion lookup, pending-mempool lookup and new-entry identity accounting; the one required proportional identity fold, replacement-removal decrement derivation, canonical encoding, admission cryptography and successful-receipt SHA3 remain. One Vec-to-Arc conversion per distinct frame or scheduler pump, atomic refcounts and queue retention, one store-owned encoded-envelope `Vec` per block actually returned, one final owned vector per accepted sync envelope and the bounded outer response vector, kernel/libp2p copies, additional write polls/syscalls, necessary dual-transport ownership, non-preemptible verification/transition, canonical encoding, bounded scans, decoded heap overhead, multi-identity partitioning, NAT sharing and Sybil resistance remain open. See WAVE-87-NET04-PREPARED-BLOCK-BROADCAST.md, WAVE-90-EN08-SHARED-DEVNET-FRAMES.md through WAVE-93-EN08-STREAMED-LIBP2P-SYNC-RESPONSES.md, WAVE-94-NET04-P2P-SYNC-BYTE-PREFLIGHT.md, WAVE-113-EN08-STREAMED-SYNC-DECODE.md, WAVE-123-EN08-DIRECT-BLOCK-FRAME.md, WAVE-126-EN08-FIXED-SYNC-REQUEST.md, WAVE-130-EN08-STREAMED-TRANSACTION-INJECTOR.md, WAVE-134-EN08-OWNED-BLOCK-GOSSIP-PAYLOAD.md, WAVE-138-EN08-OWNED-TRANSACTION-GOSSIP-PAYLOAD.md, WAVE-149-EN08-BLOCK-QUEUE-LENGTH.md, WAVE-152-EN08-ATTESTATION-QUEUE-LENGTH.md, WAVE-157-EN08-INCREMENTAL-ORPHAN-EVICTION.md, WAVE-160-EN08-INDEXED-FINALITY-ORPHAN-CLEANUP.md, WAVE-164-EN08-INDEXED-FINALITY-REFUSAL-FIFO.md, WAVE-168-EN08-BORROWED-DEEP-REPLAY.md, WAVE-172-EN08-SINGLE-RPC-TRANSACTION-OWNER.md and WAVE-177-EN08-SINGLE-ADMISSION-TXID.md. | The shared engine queue budget has no per-peer fairness: one peer can keep honest blocks and attestations shed |
| EN-09 | PARTIAL | Pruning removes descendants disconnected by the finalized floor. Live gossip now retains at most two authenticated proposals per immutable genesis-key duty, preserving an equivocation pair while ignoring later variants before body/fork-choice retention; exact ids prevent double charging across future/orphan promotion. Growth remains rate-bounded rather than absolute if finality stalls, deposit-added identities remain exempt until parent-derived authentication is available, and multiple keys multiply the rate. See TRANSACTION-REPORTING-AND-BRANCH-RETENTION.md and WAVE-65-EN09-PROPOSAL-DUTY-RETENTION.md. | A validator key can grow `blocks` (a fork-choice input) without bound above the finalized floor |
| EN-10 | PARTIAL | The slot loop now detects lag before any duty, quarantines both attestation and proposal signing across multi-page progress, requests sync first, and reports the validator inactive while quarantined. A bounded quiet-window escape preserves recovery after genuinely empty slots because the legacy transport has no end-of-page marker; transport-level completion evidence and fleet qualification remain. | Duties are signed against an artificially rolled stale state while the node is behind |
| EN-11 | IMPLEMENTED | Pending status uses the cached txid index. | `gettxstatus` hashes every mempool transaction on the consensus thread |
| FC-04 | UNARMED CANDIDATE | Two existing inactive gates cover the finding's two mechanisms: the ancestry-seed candidate reads the already-frozen E-2 boundary instead of the trailing E-1 mix, and Rewards V2 charges measurable issuance credit for withheld scheduled proposals. Both remain `u64::MAX`, and neither is safe to activate alone from this ledger update. Joint replay, grinding analysis, economics and mixed-fleet qualification plus an explicit activation plan remain required. See WAVE-46-FC04.md. | RANDAO grinding of the *next* epoch's committees and proposer schedule is free and one epoch too close (F6 open, proposer reward absent) |
| FC-05 | UNARMED CANDIDATE | A deliberately inactive node candidate prefers the earlier signed header slot when different-slot siblings have equal weight, then retains the historical larger-root fallback for same-slot siblings. An attack regression proves a next-slot proposer can grind today's body/root tie and that the candidate removes only that exact lever. The gate remains `u64::MAX`; same-slot grinding, balancing, and the absence of proposer boost remain unresolved. See WAVE-47-FC05.md. | LMD-GHOST has no proposer boost and a root-value tie-break: ex-ante reorgs and balancing are cheap at 2 attesters per slot |
| FC-07 | PROTOCOL DECISION | A new two-branch regression proves the live 1/2 floor lets disjoint one-third validators finalize conflicting epoch-18 checkpoints without either validator equivocating. This is the documented liveness-over-uniqueness choice, not an implementation defect with a neutral fix: uniqueness requires a floor strictly above 3/4 and therefore gives up leak-driven recovery when half or less of stake remains available. No gate or candidate was added. See WAVE-48-FC07.md. | With the 1/2 floor armed, any two disjoint sets holding ≥ 1/3 of unleaked stake each finalize conflicting checkpoints after ~25 epochs, with no slasha… |
| FC-08 | PARTIAL | The inactive ancestry-seed candidate removes the seed half by reading E-2, which is frozen before E-1 begins, and node judgment already anchors committee membership to the attestation's branch. The source-checkpoint half remains: consensus refuses previous-epoch attestations, so a late E-1 tail block can still leave first-slot E votes with a stale source. Late-inclusion semantics require a separately gated protocol design and replay qualification. See WAVE-46-FC08.md. | Sub-epoch duty-view lag: with `back = 1` the seed and the source checkpoint for epoch E depend on the *last block* of E−1, which honest first-slot att… |
| INF-01 | PARTIAL | The repository defines a canonical `/build` container candidate pinned to a Rust image digest, Debian archive snapshot, commit archive, lockfile, build stamp and commit timestamp. Its comparator fails closed on symlinked, externally hard-linked or same-object artifacts, unsafe write modes, extra top-level entries and noncanonical manifest/metadata bytes. Before reading artifacts from each supplied builder tree, the comparator also requires each of `bloch-pos`, `SHA256SUMS` and `BUILD-INFO` in that tree to have exactly one hard link; cross-directory and `-ef` checks remain additional defenses. The wrapper captures one commit OID, builds from an archive of that exact object in a private source snapshot, and requires the complete canonical eight-line `BUILD-INFO` order with unique value-bound commit/date/snapshot/target/digest/kind and false signing/authorization fields. Before accepting any exported bytes, it requires the export tree to contain exactly those three top-level entries and each entry to be a regular non-symlink file with exactly one hard link and without group/other write bits, and fails closed if the checksum command fails or its extracted output is not exactly one 64-character lowercase hexadecimal value. The candidate packager likewise builds its captured archive and publishes only an exact two-line version response whose first line names the captured commit and whose second line has the asserted clean-source digest shape. It resolves one structurally validated root/node toolchain pin from the captured snapshot, requires the active compiler version and one structural host target, accepts only a validated lowercase 64-hex digest, normalizes the staged directory/binary/metadata modes, and hashes the single staged executable both before and after its version query so synchronous self-mutation is refused. The legacy same-path integrity guard refuses staged or unstaged tracked source before building, requires both double-build outputs to be regular non-symlink files with exactly one hard link before hashing or execution, and fails closed unless each checksum command succeeds with an extracted output of exactly one 64-character lowercase hexadecimal value; a post-preflight race and untracked inputs remain explicit residuals. Before publication, the canonical wrapper requires the staged root itself to be a real non-symlink directory, creates an unpredictable ownership token inside it, and after the portable move requires both a real non-symlink output root and that token as its direct child before removing it and reporting success; raced destination nesting and an observed stage-root symlink therefore fail closed. Local fake-engine, packager, integrity and comparator adversarial self-tests pass. These changes prove only supplied local candidate bytes, regular/non-symlink single-link export, comparator and integrity inputs at local preflight, basic POSIX write bits and textual metadata/context coherence; they do not exclude bind-mount or other aliasing that does not increment link count, authenticate ownership/group, ACLs/xattrs/mount policy, Git objects, engine, image, toolchain or builder, guarantee later mode preservation, or prevent an owning/background process from mutating bytes after the final check, make portable directory publication an atomic no-overwrite operation, prevent a detected collision from leaving the owned stage nested under an unowned raced destination, or cover same-owner/privileged path replacement and power loss. Two independently authenticated Linux builds and comparison, hosted CI, builder/image/tool provenance, signing, publication, independent approval, rollback rehearsal, staged canary and fleet `/proc` digest evidence remain mandatory external gates. See WAVE-49-INF01.md, the Wave 88–94 and 96–98 INF-01 comparator reports, WAVE-100-INF01-BUILD-WRAPPER-MANIFEST.md through WAVE-108-INF01-PACKAGE-SOURCE-SNAPSHOT.md, WAVE-110-INF01-RELEASE-INTEGRITY-CLEAN-SOURCE.md, WAVE-112-INF01-PACKAGE-VERSION-IDENTITY.md, WAVE-115-INF01-PACKAGE-TARGET.md, WAVE-119-INF01-PACKAGE-DIGEST.md, WAVE-120-INF01-PACKAGE-TOOLCHAIN-PINS.md, WAVE-124-INF01-PACKAGE-MODES.md, WAVE-128-INF01-PACKAGE-VERSION-BYTES.md, WAVE-146-INF01-WRAPPER-DIGEST.md, WAVE-150-INF01-INTEGRITY-DIGEST.md, WAVE-154-INF01-WRAPPER-REGULAR-FILES.md, WAVE-158-INF01-WRAPPER-EXACT-TREE.md, WAVE-162-INF01-WRAPPER-WRITE-MODES.md, WAVE-166-INF01-WRAPPER-HARDLINKS.md, WAVE-170-INF01-COMPARATOR-HARDLINKS.md, WAVE-174-INF01-INTEGRITY-BINARY-ALIASES.md and WAVE-179-INF01-WRAPPER-PUBLICATION-OWNERSHIP.md. | The live validator binary is built and shipped by a pipeline that exists only outside the repository — HIGH — status: partially KNOWN (`deploy/RELEASE… |
| LG-01 | PARTIAL | A fail-closed two-archive intake now distinguishes an applied 39,917 tip from a selected 39,918 block with missing effects, while pinning the shipped root/count/total/digests and refusing disagreement. It does not authenticate operator claims. Raw signed evidence from both frozen snapshot nodes is still required; a proved missing selected block would remain an explicit loss/remedy decision. No balance changed. See WAVE-48-LG01.md. | The carried ledger holds exactly 39,917 Genesis-3 subsidies, not 39,918; the missing coinbase is unexplained and two concrete mechanisms exist in the … |
| NET-04 | PARTIAL | Exact cryptographic failures avoid re-verification; transported work shares bounded aggregate/source verification and retention budgets whose identities survive queues and promotion. Receive paths attach exact raw-byte charges only after canonical decoded-size equality and reuse them for later queue accounting. Future/orphan/finality-refusal retention remains count/byte bounded with cached exact lengths and private authentication proofs. Locally produced blocks use one private prepared frame/id pair, avoiding a retained-envelope clone and libp2p re-decode while preserving generic-frame fallback and exact wire bytes. Directed libp2p sync applies its exact framing-aware page budget after canonical header validation but before reading or allocating the first rejected boundary body; returned bytes/order and public/devnet store page semantics are unchanged, while the pre-existing historical first-oversized empty-page recovery livelock remains. Remote connection-lifecycle diagnostics share an eight-per-ten-second limiter with exact suppression metrics; disconnect bookkeeping, source cleanup and sync recovery remain unconditional. Fresh admission/transition still verify independently; canonical boundary encoding, generic-frame decode, one dual-transport byte clone, served-body ownership, bounded scans, decoded heap overhead, Sybil/NAT sharing, non-preemptible work, peer policy and globally shared diagnostic detail remain open. See WAVE-87-NET04-PREPARED-BLOCK-BROADCAST.md, WAVE-88-NET04-CONNECTION-LOG-CHURN.md and WAVE-94-NET04-P2P-SYNC-BYTE-PREFLIGHT.md. | Unauthenticated devnet frames buy bounded but sustained consensus-thread CPU (hybrid verifies) and unbounded log spam; `Reject` has no consequence on … |
| NET-05 | IMPLEMENTED | Shared per-IP and global get-blocks budgets plus aggregate page-byte limit. | Devnet `get-blocks` serving is rate-limited per *connection* only (no per-IP/global cap), pages are 4× larger than libp2p's and carry no byte cap |
| NET-09 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Devnet idle-close at 120 s silently drops the first broadcast after an idle period; the honest cadence on non-sync connections is ~16 minutes |
| NET-10 | IMPLEMENTED | JSON value-count cap and bounded scalar IDs. | RPC JSON parser memory amplification and id echo |
| NET-11 | IMPLEMENTED | Eight RPC connections per IP; overflow sockets are dropped without blocking the accept loop. | RPC connection exhaustion/slowloris: 64 slots, 30 s deadline, no per-IP limit |
| NET-12 | PARTIAL | Observer compromise still grants an allowlisted frame-push position, but per-source queue reservations, per-IP connection/sync budgets, exact-failure caching/log suppression, authenticated-before-body proposal admission, SSH hardening and public-RPC refusal bound several consequences and reduce host exposure. Transport identity/origin, peer penalties and fleet firewall evidence remain open. See WAVE-33.md. | Bootnode/observer hosts are a privileged frame-push position into all 63 validators |
| SR-02 | PARTIAL | External checkpoint onboarding now refuses an unpinned signer-arrangement file. Mandatory `--ws-signer-set-sha3` authenticates every arrangement byte before its keys/quorum/review clock can authorize an envelope. Independent pin distribution and adoption remain external, and the V1 checkpoint signature still binds only the numeric arrangement ID; versioned digest binding remains unimplemented. See WAVE-50-SR02.md. | The signer arrangement (keys + quorum rule + review clock) is not bound by the checkpoint digest |
| SR-03 | OPEN | Release-side `ws-verify` now refuses a cryptographically valid but expired envelope whenever an RPC/current epoch is supplied, and `--require-fresh` also fails when no clock is available. Boundary regressions match the boot window and the ceremony runbook uses the fail-closed form. The repository still has no signed envelope or signer set, so fresh-install onboarding remains refused and the finding stays open. See WAVE-48-OPERATIONS.md. | Fresh-install onboarding is currently refused: the genesis anchor aged out at epoch 2016 and no signed envelope exists anywhere in the tree |
| ST-03 | UNARMED CANDIDATE | A deliberately inactive slashing-economics candidate feeds the correlation window with penalty × offender effective exposure from the same frozen capped/leaked roster as the denominator. A transition boundary excludes pre-activation raw-loss entries from V2 pricing without rewriting committed history. The `u64::MAX` gate preserves current replay; activation still needs economic review, simulation and a coordinated release. See WAVE-48-ST03-ST04.md. | Correlated-slashing amplification mixes raw-bond penalties (numerator) with effective, capped/leaked stake (denominator): one slash of a cohort valida… |
| ST-04 | UNARMED CANDIDATE | The same inactive gate makes self-slashing lock residue through E+EXIT_DELAY+WITHDRAWAL_DELAY, matching a same-epoch voluntary exit instead of unlocking 32 epochs earlier. Proven evidence still ejects at E+1 without consuming voluntary churn; the cap-free mass-ejection/halt vector and same-epoch churn-marker refund remain explicit residuals requiring protocol policy. See WAVE-48-ST03-ST04.md. | Self-slashing is a faster, cap-free exit: ejection at E+1 bypasses `MAX_EXITS_PER_EPOCH`, withdrawability lands 32 epochs *earlier* than a voluntary e… |
| ST-05 | PARTIAL | Lifecycle shape/state rules run before a budgeted verifier. One validator/funding identity gets at most two new hybrid authorization calls per 30-second wall slot, and all identities now share a 256-call aggregate ceiling; exact cached failures consume none, withdrawals consume none, and lifecycle variants have mempool source identities. This fixes the local cost bound across identity cycling, but unauthenticated peers can consume the shared allowance first and there is no cross-node penalty; transport authentication/fair scheduling remain open. See WAVE-59-ST05-GLOBAL-BUDGET.md. | Post-L, unauthenticated lifecycle transactions cost the node 1–4 hybrid verifications each before any per-source accounting; `tx_source_hash` returns … |
| TX-02 | PARTIAL | Transfer/funded-deposit budgets checked before execution; all classes checked after each charge. Unmetered legacy classes remain outside this fix. | Per-block byte/gas caps are enforced only after all transactions have executed; a scheduled proposer can force ~16× a maximal block's execution work p… |
| TX-06 | PARTIAL | Live node admission refuses zero/dust outputs and more than 256 outputs before signature work. Consensus carries the same tested rule behind inert `DUST_RULE_ACTIVATION_EPOCH`; historical compatibility and a coordinated flag day still block activation, so a hand-built proposer body remains valid today. | Zero-value and arbitrary-count outputs are valid: permanent state growth at ~6 sat per entry |
| TX-08 | UNARMED CANDIDATE | The existing fee/stake gate already stages all three coordinated rules: producer credit capped at 100 BLCH per block, excess burned by omission, and boundary fee income moved to a committed withdrawable ledger instead of compounding into consensus weight; combined own/delegated weight is also capped. The gate remains `u64::MAX`. Closed-gate and forced-gate regressions pass; replay, economics and mixed-fleet qualification plus an explicit activation decision remain required. See WAVE-46-TX08.md. | Producer fee share compounds into `staked_sat` (consensus weight) with no per-block cap |
| TX-11 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | Slashing is unreachable; equivocation is free |
| BV-05 | PARTIAL | API and new checked low-level builders require at least 144 CSV blocks; checked unvault/branch-A validation also rejects unsafe role keys. Historical unchecked builders remain byte-compatible for funded-output recovery. | `csv_delay = 0` (and any tiny Δ) is accepted everywhere; branch A becomes immediately spendable and the vault silently provides no window |
| BV-06 | IMPLEMENTED | Reject empty/oversized PQ keys before the panicking wrapper. | Remote-triggerable panic in `pq-shield-api` `/anchor/commitment`: an empty or >8192-byte `pq_recovery_pubkey` reaches the *panicking* `anchor_guard_go… |
| BV-07 | PARTIAL | Verification requires an externally trusted PQ key and authenticates all committed fields; the format provides an opaque policy field, while documentation now states that freshness, ownership and revocation are not established. No canonical sequence, expiry, predecessor or registry ordering exists, so relying-party/product integration must supply those semantics. See WAVE-36.md. | The anchor has no freshness / rotation / revocation semantics; two valid anchors for the same vault under the same trusted key are indistinguishable, … |
| BV-08 | PARTIAL | API and opt-in low-level checked builders bound money range, subtraction, dust and fees at 10 percent of input. The new bounded clawback ladder provides independently checked, pre-signable replacement fees. Dynamic estimation, relay-policy qualification and historical unchecked builders remain open. See WAVE-49-VAULT-FEE-LADDER.md. | No dust, absurd-fee, or fee-estimation checks in the tx builders or the API; pre-signed transactions freeze fees |
| BV-09 | PARTIAL | Owned vault PQ secret vectors clear on Drop; temporary seeds, discarded BTC secrets, the repository-owned HKDF recovery-secret output and the V3 domain-separated PQ-seed digest use zeroizing ownership from before their output bytes are written. Checked restoration receives its HKDF owner directly, the public tuple helper hashes before its final compatibility copy, and SHA-256 finalizes the V3 PQ seed directly into its protected owner. `VaultKeys` is non-`Clone`, and its three secret-bearing fields are private behind borrow-only accessors; compile-fail, identity and known-answer regressions pin those boundaries. Explicit caller-owned return arrays, classical `SecretKey: Copy`, BIP32/hash/PQ backend/compiler/OS internals remain outside the guarantee. See WAVE-52-BV09.md, WAVE-54-BV09.md, WAVE-108-BV09-ZEROIZE-RECOVERY-SECRET-OUTPUT.md and WAVE-111-BV09-ZEROIZE-V3-PQ-SEED-DIGEST.md. | Secret material is never zeroized: `VaultKeys` (`Clone`), `pq_secret: Vec<u8>`, `SecretKey` (`Copy`, no `Drop`), master `Xpriv`, the HKDF IKM copy |
| BV-10 | PARTIAL | Checked restore binds the recorded V1/V2/V3 family, network, non-empty `vault_id` and `H(r)` to independently supplied funded state. An additive bounded envelope now PQ-signs the exact canonical context under a distinct domain and verifies against an independently trusted owner key before restore; key substitution, metadata/signature tampering, malformed lengths and trailing bytes fail closed. Adoption, trusted-key distribution, global ID uniqueness, reuse/single-use tracking and on-chain PQ-possession proof remain external. See WAVE-50-BV10.md and WAVE-51-BV10-SIGNED-CONTEXT.md. | `r` is deterministic in `(pq_sk, vault_id)` with a client-chosen, API-invisible `vault_id`; reuse or derivation-version confusion silently degrades or… |
| BV-11 | PARTIAL | Rejects signed-anchor trailing bytes; API validates address syntax and a common network. A new checked low-level Bitcoin verifier requires the caller's expected network, validates both committed addresses, then retains external-key and PQ-signature checks. Generic multi-chain verification and direct struct/sign construction remain format-compatible and do not infer address semantics. See WAVE-53-BV11.md. | `SignedAnchor::deserialize` ignores trailing bytes; anchor address fields are never validated as addresses |
| BV-12 | PARTIAL | JSON-only bounded API, cross-site checks and loopback-only binary bind. Authentication, TLS and per-client rates require deployment integration. | `pq-shield-api` deployment posture: plain HTTP, no authentication, no per-client rate limit, `0.0.0.0` bind supported; CSRF-shaped requests reach hand… |
| BV-13 | IMPLEMENTED | Anchoring HTTP bounds connect/read/write/overall time and response bytes; credentials require HTTPS, redirects refused, JSON-RPC response identity/shape checked. | `anchoring/src/http.rs`: no read/write timeout, API key sent in clear over whatever scheme the caller passes, lenient RPC parsing |
| BV-14 | PARTIAL | GitLab now runs native Ustav/EUVM and PQ boundary checks. Name-denylist limitations and full kernel/product qualification remain open. | The Ustav "PQ boundary" is a name-denylist tripwire that runs only in GitHub Actions; the GitLab pipeline neither runs it nor blocks on `bloch-ustav`/… |
| BV-15 | PARTIAL | Evaluator applies CSV maturity to actual input nSequence, not merely the script operand; real signature regression covers144-script/288-sequence maturity. Existing integer/conditional/stack/sighash guards retained. Bitcoin Core differential qualification remains open. | `script_eval.rs` diverges from Bitcoin Core in ways that matter for standardness, and it is the *only* thing validating the vault's scripts |
| CR-03 | PARTIAL | Transfer V1/V2 admission now refuses non-hybrid keys before signature verification through the crypto crate's length-safe suite classifier; exact legacy raw hybrid keys remain accepted. Direct block inclusion still passes generic consensus verification, so output inventory, a protocol decision and a coordinated activation gate remain required. | `SUITE_MLDSA65_ONLY` (0x0002) is accepted by the live Genesis-4 transfer verifier with no activation gate, contradicting the "hybrid on every consensu… |
| CR-04 | PARTIAL | Explicit wallet V2 hashes the BIP39 seed under a PQ-specific SHA3 domain before keygen; vault V2 uses that KDF and a dedicated branch, while V3 additionally hardens independent hot/recovery roles and uses a vault-specific PQ domain. All 38 wallet/vault tests pass. Compatibility defaults and existing V1 material still reuse the raw seed and require explicit migration. See WAVE-34.md. | Seed-derived keys are reused across contexts: `bloch-btc-wallet` default identity and `bloch-pq-vault` V1 derive the PQ key from the raw BIP39 seed, w… |
| CR-05 | PARTIAL | Current wallet and both optional/legacy CLI transaction construction reject cross-network recipients. Historical checksum network binding remains unchanged for compatibility. | Address checksum does not bind the network; `Wallet::build_tx` accepts a recipient `Address` of the other network |
| CR-06 | PARTIAL | Explicit disclosure convention supports HD-v3 index zero while preserving both old derivation families and wire format. The sole production disclosure CLI caller now names `SingleKeyWallet` explicitly; library callers must still choose the correct family rather than infer it. See WAVE-107-CR06-EXPLICIT-CLI-DISCLOSURE-CONVENTION.md. | Index-0 convention mismatch between `wallet::disclosure::keypair_at` and `hd_wallet::derive_at` |
| CR-07 | PARTIAL | Derived HD key authenticity, bounded reads/work and in-place zeroizing decrypt paths remain in place. BIP39 V1/V2 salt copies, the HD master-key KDF output, canonical mnemonic KDF copies, fresh HD generation entropy, all three repository-visible BIP39 seed owners, diversified-subseed consumers, discarded diversified-key secret outputs, HD derived-key transfer owners, the HD master-key KDF salt output, the HD decrypt success-return plaintext owner, the internal v1 keyfile decrypt return/loader handoff, current single-key wallet constructor and legacy key-generator secret-key owners, CLI mnemonic/keystore/new-password prompt owners, password-denylist normalization copies, legacy save/load KDF and plaintext temporaries, and hybrid-keygen pre-envelope secret temporaries now use zeroizing ownership without API, schema, KDF, RNG or output changes; fatal CLI branches explicitly drop their prompt owners. The v2 keyfile decrypt split additionally reuses the authenticated plaintext allocation for the private-key return, overwrites its former header/seed prefix and wipes its remaining old logical tail; the independent master-seed return remains required by compatibility. Both v1 and v2 keyfile decrypt paths now apply their unchanged `m_cost`, `t_cost` and `p_cost` ceilings immediately after version/algorithm validation and before any Base64 decode, avoiding decoder output for inputs already refused by KDF policy while preserving the in-policy validation order and bytes. The separate legacy `Keypair` loader now applies its unchanged memory, time, parallelism and 32-byte-output policy immediately after version validation and before any Base64 decode; `derive_key_with_params` retains the same validation as defense in depth, while in-policy decoding, KDF, AES, schema and output remain unchanged. Disclosure verification now rejects standard padded-Base64 fields beyond encoded bounds derived from the existing 8 KiB public-key and 16 KiB signature decoded caps before decoder-output allocation; the post-decode caps remain authoritative and accepted schema/digest/signature behavior is unchanged. `postern-wallet verify-bundle` and `watch` now share a 64 MiB file budget before Serde and read at most one probe byte beyond it; canonical/ordinary serialization of the bounded typed fields retains ample headroom, while semantically equivalent oversized whitespace/unknown-field JSON is intentionally outside the CLI policy. The by-value array returned by `bip39` before its immediate RAII move, public seed/key-return compatibility APIs and caller-owned outputs, terminal/OS/`rpassword` buffers, `bip39` and cryptographic/KDF/cipher backend state, caller-owned inputs to the public in-memory verifier, the admitted CLI byte buffer and Serde-owned typed strings, payloads with in-policy KDF parameters and other bounded JSON/Base64 input allocations materialized before verification, allocator/compiler/register copies, and aborts that skip destructors remain open. See WAVE-87-CR07-ZEROIZE-DIVERSIFIED-SUBSEED.md, WAVE-88-CR07-ZEROIZE-CLI-PROMPT-SECRETS.md, WAVE-89-CR07-ZEROIZE-NEW-PASSWORD.md, WAVE-90-CR07-ZEROIZE-GENERATION-ENTROPY.md, WAVE-92-CR07-ZEROIZE-LEGACY-SAVE-TEMPORARIES.md, WAVE-93-CR07-ZEROIZE-LEGACY-LOAD-KDF.md, WAVE-94-CR07-ZEROIZE-HYBRID-KEYGEN-TEMPORARIES.md, WAVE-114-CR07-ZEROIZE-HD-MASTER-KEY-OUTPUT.md, WAVE-118-CR07-ZEROIZE-HD-KDF-MNEMONIC.md, WAVE-122-CR07-ZEROIZE-HD-GENERATION-ENTROPY.md, WAVE-127-CR07-ZEROIZE-HD-SEED-OWNERS.md, WAVE-131-CR07-ZEROIZE-WALLET-SECRET-OWNERS.md, WAVE-133-CR07-ZEROIZE-DISCARDED-DIVERSIFIED-SECRET.md, WAVE-136-CR07-ZEROIZE-LEGACY-KEYGEN-SECRET.md, WAVE-142-CR07-ZEROIZE-NORMALIZED-PASSWORD.md, WAVE-145-CR07-ZEROIZE-HD-DERIVED-SECRET.md, WAVE-148-CR07-ZEROIZE-HD-MASTER-SALT.md, WAVE-153-CR07-ZEROIZE-HD-DECRYPT-RETURN.md, WAVE-156-CR07-ZEROIZE-KEYFILE-CONSUMER.md, WAVE-161-CR07-ZEROIZE-V2-DECRYPT-SPLIT.md, WAVE-165-CR07-DISCLOSURE-BASE64-PREFLIGHT.md, WAVE-169-CR07-BOUNDED-DISCLOSURE-CLI-READ.md, WAVE-173-CR07-KDF-BOUNDS-BEFORE-BASE64.md and WAVE-176-CR07-LEGACY-KDF-BOUNDS-BEFORE-BASE64.md. | Wallet-library robustness nits (panics / zeroization gaps on untrusted or edge inputs) |
| CR-08 | PARTIAL | Production key generation and every repository KAT now use the scoped seeded-RNG API. The manual guard and its type are private, so downstream callers cannot forget one and leave deterministic entropy active; return, unwind and TLS teardown cleanup remain covered. Caller seed copies and opaque ChaCha internal-state erasure remain unresolved. See WAVE-51-CR08.md. | `SeededRngGuard` design hazards: a forgotten guard seeds the thread forever; ChaCha state is not zeroized; `Drop` touches TLS unconditionally |
| EN-12 | PARTIAL | Held attestations use their epoch and target seed; duty index is cleaned on release. Complete branch-historical registry projection remains open. | `release_held` judges released attestations with the *wall epoch's* seed and roster, not the attestation's |
| EN-13 | IMPLEMENTED | Exact duplicate parked orphan/future blocks skip repeated hybrid verification; regression coverage. | Re-offered orphans pay a hybrid verify on every delivery |
| EN-14 | PARTIAL | Remembered refused ancestry suppresses child requests and prunes parked descendants. Unseen/evicted ancestry remains unresolved. | Orphans hanging off a latch-refused branch keep the sync pump broadcasting `get_blocks` to all peers indefinitely |
| EN-15 | IMPLEMENTED | Known-parent non-increasing slots rejected before storage insertion; slot-monotone ancestry regression. | `ancestral_boundary_mix` walk is bounded by `blocks.len()`, and stored non-canonical blocks are not slot-monotone |
| EN-16 | IMPLEMENTED | Global/unattributed proposal errors narrow the current candidate without evicting or barring innocent mempool entries. | The proposer's drop loop bars innocent transactions on non-indexed transition errors |
| EN-17 | IMPLEMENTED | Live reorgs queue whole-log encoding, write, fsync, index invalidation, atomic publication and index rebuild on a dedicated writer. The engine remains responsive to RPC/network work, blocks validator duties until durability, joins before any later append or orderly shutdown, writes the restart cache only after completion and fail-stops on writer errors. Crash ordering and append/Drop joins have regressions. Production-scale latency/RSS remain unqualified; see WAVE-43-STORAGE.md. | `do_reorg` rewrites the whole block log synchronously on the consensus thread |
| EN-18 | PARTIAL | At most 16 outstanding engine RPC calls, including timed-out queued work. EUTXO and validator-registry-only methods use SharedHead; static `getbuildinfo` and a complete canonical `getblockcount` snapshot now answer without queue capacity or consensus-thread work. The block-count snapshot is published after apply, accepted reorg and cache restore, with one formatter shared by the published and engine-fallback paths. A racing reader may see the previous complete head. Block cloning/serialization, chaininfo, mempool/status reads and submissions remain on consensus. See WAVE-52-NET01.md, WAVE-53-EN18.md and WAVE-55-EN18.md. | RPC events bypass the queue budget and are answered on the consensus thread |
| EN-19 | IMPLEMENTED | Automatic lifecycle signing respects doppelganger protection and persists identity-bound RANDAO intent before signing. V3 backup/import preserves monotonic intent and recovery floors; older binaries refuse V3. Past signatures and unfenced copies remain operational risks. | `RandaoRecommit` is signed outside slashing protection and outside the doppelgänger gate |
| FC-06 | PARTIAL | Orphans are held in a separate bounded queue and enter `blocks` only after their parent lands, so they no longer feed node fork choice; finality-shaped pruning removes stale branch descendants. The bare `Store::observe` remains order-dependent for an equivocating validator's A/C/B sequence. A bounded committed vote horizon and six-order regression exist behind the unarmed `FORKCHOICE_EQUIVOCATION_HORIZON_ACTIVATION_EPOCH`; coordinated activation or a separately reviewed node-side redesign remains required. See WAVE-41.md. | Node-side fork choice is order- and store-shape-dependent (O01), and feeds *every stored block* including orphans |
| FC-09 | PARTIAL | Out-of-order finality processing increments a saturating failure counter and emits bounded-frequency diagnostics. Regression exercises real desynchronized state. No consensus reset or recovery policy is introduced; see FINALITY-EPOCH-OBSERVABILITY.md. | `close_epoch` silently swallows `FinalityError::OutOfOrderEpoch`; a desynchronised engine would stop finality forever with no signal |
| FC-10 | UNARMED CANDIDATE | Canonical `DS_NETSIG2` candidate roots bind the unchanged historical attestation/proposal roots to the committed genesis-manifest domain; cross-genesis and legacy-stability tests pass. Production signing, validation, gossip identity and slashing evidence remain on historical roots behind `VALIDATOR_NETWORK_BINDING_ACTIVATION_EPOCH = u64::MAX`. Coordinated wiring, mixed-fleet replay and activation remain required. See WAVE-46-NETWORK-SIGNING-BINDING.md. | Attestation and proposal signing roots bind no network/genesis identity |
| FC-11 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | The leak is absolute (satoshis) while `duty_roster_at` rescales effective stake; a residual leak can zero a validator the moment a cap binds |
| FC-12 | PARTIAL | The node now exports the canonical committed bar's historical count, active count and exact leak-adjusted active stake, and emits a bounded diagnostic whenever that summary changes. A regression pins the current semantics: exit and epoch rollover remove live weight but do not erase the historical bar. No expiry was added: current state records no offence epoch, and restoring weight needs a specified interaction with slashing, exit/re-entry, retained evidence and historical bars. Consensus, replay and state-root bytes are unchanged. See WAVE-47-FC12.md. | Committed `fc_equivocators` bar is permanent and has no exit/slash/recovery path |
| INF-03 | PARTIAL | Legacy manifest paths and hardened arithmetic failures are repaired. GitLab matches GitHub's blocking lifecycle/partition/activation/admission/joining proofs under exact ordered contracts and uses a locked workspace build. Required job and execution-authority keys must be unique/plain; both guards reject quoted/escaped, explicit, tagged, anchored, aliased, merge-key and flow mappings, plus YAML directives and early/late document boundaries, outside block scripts. Local selftests pass 175/175 test-posture and 136/136 scanner-posture cases; live guards cover all eight live crates on both checked-in pipelines and 8+8 security jobs. This is a local textual-subset proof only: hosted parsing/execution, event delivery, runner/image/tool authenticity or availability, project rules, branch protection and inherited formatting drift remain unqualified; GitLab fmt remains informational. See WAVE-86-INF03-GITLAB-ROOT-MERGE.md and WAVE-87-INF03-YAML-DOCUMENT-BOUNDARIES.md. | GitLab's `check` stage is red on `main`, so its `test` stage never runs; the "both pipelines gate the live crates" claim is false — MEDIUM — NEW |
| INF-04 | PARTIAL | GitLab adds blocking native Ustav/EUVM and boundary checks and matches GitHub's lifecycle/partition/activation/admission/joining proofs. Exact guards bind commands, attested-SSH and installer-ISO proofs. Both meta-guards require unique plain required-job/global execution keys; disguised duplicates and YAML merge authority cannot replace reviewed GitHub or GitLab context/jobs/scripts. Both CI documents reject unsupported mapping-key syntax globally outside block scripts; quoted scalar values, GitLab flow sequences and block data remain supported. GitHub also binds full events, read-only token authority, runner label and no suppressing dependency. Complete pipeline parity, hosted execution, runner/image state and branch protection remain open. See prior INF-04 reports and WAVE-86-INF03-GITLAB-ROOT-MERGE.md. | GitHub and GitLab pipelines diverge; the pipeline this clone actually pushes to is the weaker one — MEDIUM — NEW |
| INF-06 | PARTIAL | SP1 candidate recipe pins base-image digests and downloaded toolchain SHA256, removes pipe-shell installers/whole-tree COPY, isolates guest workspace and commits its lock. Guest dependency resolution passed; Linux/container/GPU and actual proof qualification remain open. See deploy/sp1-prover/BUILD-INPUTS.md. | SP1 prover image: pipe-to-shell installers, floating base images, whole-repo `COPY` — MEDIUM — NEW |
| INF-07 | PARTIAL | Updated activity metric follows current registry/signing gates; corrected startup false alarms and added availability/heartbeat/store/inactivity/restart rules with target labels. Deployment, runtime rule qualification and exact missed-duty coverage remain open. | Alerting has holes and the alert docs contradict the exporter — MEDIUM — NEW |
| INF-08 | IMPLEMENTED | Host-loss and checkpoint runbooks use actual JSON-RPC/CLI interfaces and distinguish observation from old-host fencing. | Runbooks name a CLI and an RPC method that do not exist, on the host-loss fencing path — MEDIUM — NEW |
| INF-09 | PARTIAL | The post-audit base provides a fail-closed rollback assembler, detached minisign authentication over every root-reaching file, an out-of-band trust-root contract, a generated systemd installer that verifies the running `/proc` image, and adversarial tamper tests. Assembly now snapshots the caller path once into private storage and derives version, binary hash, manifest, installer, README and signed trusted comment from the resulting byte identity, closing the reproduced input-path/self-mutation split. Every assembler SHA-256 observation now fails closed unless it is exactly one lowercase 64-hex digest; manifest values are validated before formatting, and the privately built tarball is digest-validated before tarball/public-key publication. Publication now copies both validated outputs to exclusive temporary files on the output filesystem, refuses pre-existing final names, creates the public-key and then tarball completion names with atomic no-overwrite hard links, and on controlled failure removes a final name only when inode identity proves that this invocation owns it. Both GitLab and GitHub run the disposable-key test as a blocking job, and the shared posture guard refuses deletion/waivers. The checksum/minisign executables and assembler host, a real signing key, host pinning, scratch-systemd drill, staged N-1 artifact and fleet exercise remain external release evidence; publication is still sequential rather than a multi-file transaction, so SIGKILL, power loss or filesystem failure can leave residue. See WAVE-27.md, WAVE-109-INF09-ROLLBACK-BINARY-SNAPSHOT.md, WAVE-137-INF09-ROLLBACK-DIGEST.md and WAVE-140-INF09-ROLLBACK-PUBLICATION.md. | No rollback is currently possible, and the release signing flow is unspecified — MEDIUM — KNOWN (`deploy/RELEASE-INTEGRITY.md` §8.7), restated because… |
| INF-10 | PARTIAL | The external bootnode verifier now fails if published hosts expose RPC on 8080, 16310 or 16400, and an adversarial self-test is blocking in both CI systems. This proves the guard, not the current fleet state: no live host was probed or changed. See WAVE-28.md. | Bootnodes expose the full unauthenticated PoS JSON-RPC (incl. `sendrawtransaction`) on `:8080`, contradicting the published posture — MEDIUM — status:… |
| INF-11 | PARTIAL | Required scanner/test jobs reject failure waivers, unsafe context and cross-step replacement. Required jobs/global authority blocks are exact contracts. Disguised YAML keys fail closed across both CI documents, including quoted/escaped, explicit, tagged, anchored, aliased, merge and flow forms; block-scalar data remains data. Local entrypoints/load edges carry integrity checks. Python uses isolated mode; both pipelines validate Rust pins and clear shell/Python/Rustup/Cargo/compiler substitution channels. GitLab jobs cannot override inherited startup context; GitHub requires exact triggers/token scope/runner and no suppressing dependency. OSV scope equals all indexed lockfiles. Scanner/test suites now pass 128/167 adversarial fixtures and real guards cover 8+8 security jobs plus all eight live test crates on both pipelines. Actions, directories/`$HOME`, actual tool/runner-image bytes, hosted availability/settings/execution and rulesets remain outside proof. See prior INF-11 reports and WAVE-86-INF03-GITLAB-ROOT-MERGE.md. | The `check-*-blocking` guards are bypassable by trivial spellings and cover only five jobs — LOW — NEW |
| INF-12 | IMPLEMENTED | Installer requires committed platform/version SHA256 pins, verifies before extraction and atomically replaces cached binaries. CI executes the verified absolute path. Offline bypass regressions pass; pins originate from upstream HTTPS, not independent signatures. | Scanner binaries are pinned by version, not by hash; a pre-existing binary on the self-hosted runner is trusted blindly — LOW — NEW |
| INF-13 | IMPLEMENTED | Docker context exclusions for common key/passphrase files; existing history/images are not cleaned. | `.dockerignore` does not exclude key material; only `COPY . .` makes it bite — LOW — NEW |
| INF-14 | PARTIAL | Actual scalar must carry an exact digest; comment exemptions are removed, and only the non-pullable template sentinel plus one same-service build/never-pull fixture are allowed. YAML anchors, aliases and merge keys now fail closed rather than escaping the explicit scalar scan, with mutation coverage. CI commentary now matches the green repository state. Generated configurations, registry signatures/provenance and arbitrary non-YAML templating remain outside this structural guard. See WAVE-53-INF14.md. | Image-pin guard exemptions are loose and its own docs are stale — LOW — NEW |
| INF-15 | PARTIAL | Retired local compose RPC mappings bind loopback. Explorer/pool proxy now requires configured HTTPS without public wildcard fallback, bounds bytes/time and refuses redirects. Live TLS, DNS and origin firewall qualification remain pending. | Retired-but-deployable configs publish unauthenticated RPC; explorer upstream is plain HTTP via a third-party DNS — LOW — NEW (explorer part is KNOWN … |
| INF-16 | IMPLEMENTED | Rehearsal uses repository-owned serialization wrapper and refuses a missing wrapper; owner/mode checked cooperative lock replaces world-writable temporary executable. | `scripts/prova-relanca.sh` executes a wrapper from world-writable `/private/tmp` — LOW — NEW |
| INF-17 | PARTIAL | Attested appliances disable sshd by default. Cloud explicitly opts into key-only, non-root SSH with port 22 allowed only on wg0. Both CI definitions run mutation-tested attested-SSH and installer-ISO structural guards; the required test-posture contracts now fail if either selftest or real verdict is removed, and reviewed hashes/transitive-load edges bind the guard executables. The Nix proof checks both composed attested configs. The mixed repart key-file/crypttab TPM2 persist design remains unqualified without Nix build, LUKS token and reboot evidence; see WAVE-16.md, WAVE-80-INF04-ATTESTED-SSH-POSTURE-PARITY.md and WAVE-80-INF17-ISO-HARDENING-POSTURE-PARITY.md. | The attested/appliance image keeps sshd enabled with NixOS defaults; the persist-volume encryption design is internally inconsistent — LOW — NEW |
| INF-18 | PARTIAL | PoS module requires explicit package rather than falling back to legacy PoW binary; mesh transport wiring added. The retained module is labeled Genesis-3 and its default RPC port matches the binary. Other Nix modules and real NixOS evaluation remain open. | Nix modules that cannot work as shipped — LOW — mostly KNOWN (admitted TODOs) |
| INF-19 | PARTIAL | Both CI definitions run blocking tracked-source and full-depth reachable-history scans with reviewed redacted baselines. Source exceptions additionally bind reviewed file SHA256. Real scanner bypass/reintroduction tests pass. Deleted/unfetched refs, old leak/rotation claims and hosted CI remain unverified; see HISTORY-SCAN.md. | History claims (`catalog-dev.secret.pem`, "55 findings", leaked PAT) cannot be verified from this clone; CI never scans history — LOW — status: KNOWN … |
| INF-20 | PARTIAL | Ceremony passphrases already use an inherited descriptor rather than exported environment state. Carryover tests now use hermetic temporary directories, Rust pins come from the checked-in toolchain files, and the shell-runner rollback job no longer invokes runtime `sudo` to install minisign. Adversarial pin/parser and ceremony tests pass. Shell-runner enrollment, fork-pipeline policy, and provisioned minisign provenance remain external operational controls. See WAVE-45-INF20-BV18.md. | Minor key-handling and hygiene items — LOW — NEW |
| KS-01 | IMPLEMENTED | Key generation takes the directory lock and refuses existing validator.key. | `keygen` silently overwrites an existing `validator.key` (no `create_new`, no lock, no confirmation) |
| KS-02 | IMPLEMENTED | Production passphrase-file input uses the same permission checks as sealing. | Production passphrase file (`BLOCH_KEYSTORE_PASSPHRASE_FILE`) is not mode-checked; only the `keys seal --passphrase-file` path is |
| KS-03 | IMPLEMENTED | All new sealed writes require 12 characters; this does not strengthen existing passphrases. | No minimum passphrase length on the `keygen` / env path; the mainnet ceremony sealed 64 keystores through it |
| KS-04 | PARTIAL | Offline bound export/import and minimum-slot CLI implemented; monotone merge and atomic private writes tested. External old-host fencing and complete prior-signature bounds remain operational prerequisites. | Slashing protection has no import/export and no "minimum slot" initialization; the host-loss runbook's fencing step cannot be executed with the shippe… |
| KS-05 | IMPLEMENTED | Native pre-thread terminal entry restores complete termios before pending termination/job-control signals and rejects interrupted partial entry. Eleven real CLI PTY cases cover dispositions/masks and stop/resume. SIGKILL/SIGSTOP and multithreaded use are outside this supported API. | Interactive passphrase entry: echo not restored on signal, `tcsetattr` restore result ignored, and `Stdin`'s buffer retains the passphrase |
| KS-06 | IMPLEMENTED | Secret hex buffers zeroized, signing key file mode checked, new secret file and directory fsynced. | `ws-sign` / `ws-signer-set` key-file hygiene: non-zeroized hex copy of the secret, no mode check on `.sk`, `.sk` write not fsynced |
| KS-07 | PARTIAL | Read-only inspection remains the first step. A new offline command can truncate only the freshly confirmed incomplete or all-zero tail, after exclusive data-directory locking and a durable create-new backup of every removed byte. It refuses non-zero complete-frame corruption, offset drift, an existing backup and a running node. Log/frame formats and automatic boot behavior are unchanged. Per-frame checksums and recovery policy for a complete frame that fails consensus replay remain open. See WAVE-49-KS07.md. | Block log has no per-frame checksum; a zero-filled tail is classified as corruption (boot refusal, no repair tool); a replayed block that fails `apply… |
| KS-08 | IMPLEMENTED | Key mutations verify ownership/type before writes/locks; reject symlinks. Read-only inspect no longer creates LOCK. | `keys seal` / `keys inspect` run as another user leave root-owned `validator.key` / `LOCK`, so the next node start fails |
| KS-09 | PARTIAL | Source/build digests cover the audited source set, compiler/Cargo/sysroot components, configured native tools, wrapper delegates, linkers and locked cc-rs controls. Workspace source identity fails closed on incomplete traversal. cc-rs compiler/archiver bytes and target-specific freestanding/WASI native trees are bound. Twenty additional implicit compiler subprogram/include/library search, Linux/macOS dynamic-loader injection/search and archive timestamp variables are watched even while absent and hashed without disclosure. Binding their values does not hash every file reachable through those paths; undeclared OS defaults, tool-loaded libraries outside hashed trees, implicit tools and independent provenance remain open. See prior KS-09 reports and WAVE-76-KS09-IMPLICIT-NATIVE-ENV.md. | Source-digest scope gaps: a compiled C include (`.macros`) and dot-directories are outside the hash; toolchain env not captured |
| KS-10 | IMPLEMENTED | Atomic private replacement plus fsync for validator.key and initial meta.bin. | Non-atomic writes: `save_with` truncates `validator.key` in place; `meta.bin` written with `fs::write` and no fsync |
| KS-11 | PARTIAL | Ordinary opening accepts only the exact production Argon2 tuple (64 MiB × 3 passes × 1 lane), so an unauthenticated header cannot select any alternative cheap or expensive cost. Historical non-production recovery requires both the explicit opt-in and one exact operator-reviewed tuple; malformed, out-of-range or mismatching expectations fail before Argon2. The 1 GiB/64-pass/16-lane hard caps remain, recovery cannot authorize new seals, and authenticated weak legacy files warn. The fixed production derivation still spends 64 MiB/192 MiB-pass before detecting a wrong passphrase or modified authenticated header. See WAVE-78-KS11-PRODUCTION-KDF-PIN.md. | Header-supplied KDF cost is honored down to the Argon2 floor with no minimum or warning; caps still allow minutes of CPU per open |
| KS-12 | IMPLEMENTED | Root toolchain pinned to Rust 1.94.1. | Toolchain pin is crate-scoped; documented root-level build commands bypass it |
| LG-02 | PARTIAL | The mismatch is now documented and regression-pinned: historical mode reproduces the committed artifact byte-for-byte, while `bloch-snapshot-utxo --canonical-vout` decodes the little-endian key correctly for new exports. Five focused tests pass. The 38 mislabelled vout=1 rows remain committed as vout=16,777,216 in the immutable Genesis-4 opening artifact; correcting live outpoints requires an explicit state migration/protocol decision. See WAVE-39.md. | vout endianness (Legacy M-3): the bug is in the exporter/`iter_utxos_sorted`, the Genesis-4 loader carries the corrupted index verbatim into committed… |
| LG-03 | IMPLEMENTED | UTXO iterator/export fail on malformed keys, undecodable values or trailing bytes; valid historical endian behavior preserved and tested. | Snapshot exporter and `iter_utxos_sorted` silently drop undecodable UTXO rows (fail-open on the ledger-producing path) |
| LG-04 | IMPLEMENTED | Canonical address before cooldown lookup. | Faucet per-address cooldown is bypassable by hex case variation |
| LG-05 | IMPLEMENTED | Require application/json and reject cross-site Fetch Metadata. | Faucet accepts cross-site form POSTs (CSRF-driven drips) |
| LG-06 | PARTIAL | Shared explorer/pool transport enforces HTTPS configuration, read allowlists, bounded requests/responses and deadlines. No plaintext wildcard fallback. Direct archival RPC exposure, origin read-only policy and live deployment remain unverified. | Explorer/pool-site RPC path: plaintext upstream via a third-party wildcard DNS, and the archival node's full unauthenticated RPC (write methods includ… |
| LG-07 | PARTIAL | Bounded staged sync rechecks linkage and anchor/tip; cancellable RPC phase has monotonic deadline, graceful shutdown and short-batch progress. Remote RPC requires HTTPS. Canonical-chain provenance, synchronous whole-state rewrites, pruning and existing-data reconciliation remain open. | Reference indexer: unbounded responses and whole-state rewrite |
| LG-08 | IMPLEMENTED | Reject non-ASCII/oversized satoshi strings and empty display values; generator updated too. | Python SDK amount parsing accepts non-ASCII digits and has an uncaught-exception path |
| NET-06 | IMPLEMENTED | Read at most 14 request bytes for the 13-byte libp2p sync message. | libp2p sync codec buffers up to 8 MiB per inbound *request* substream for a 13-byte message |
| NET-07 | IMPLEMENTED | Gossip and directed-sync blocks now carry peer/slot provenance through the engine verdict. Only an engine `Accept` from a still-connected PeerId raises its monotonic height hint; `Ignore`/`Reject` do not, and disconnect removes the hint. Shared request-ID accounting, bounded FIFO and periodic exploration remain; canonical-progress proof and Sybil resistance are explicitly not claimed. See WAVE-43-NETWORK.md. | libp2p sync steering: `peer_head` is set from the unvalidated header slot before the engine judges, and the top-`SYNC_FANOUT` claimants are always chosen |
| NET-08 | IMPLEMENTED | Identify advertisements cannot overwrite configured dial ownership. | libp2p: identify-advertised addresses overwrite the `dialed` map, letting a connected peer suppress redials to honest configured peers (eclipse assist) |
| NET-13 | IMPLEMENTED | Failure to install peer scoring is fatal at transport initialization. | libp2p `with_peer_score` failure is warn-not-fatal; a scoring-less node is a flood amplifier |
| NET-14 | IMPLEMENTED | Connection-lifetime slots replaced by finite FIFO request leases shared across timer/engine, with expired-queue authorization checks and backpressure resumption. Silent-peer/inbound-response regressions pass. Response completion, Sybil resistance and fleet recovery SLA remain separate limitations. | Devnet sync slots are sticky for the connection lifetime; a silent slot-holder throttles catch-up |
| NET-15 | PARTIAL | Legacy devnet request/serving leases retained. Libp2p now shares exact request-ID windows across connect/timer/chase, with response/failure/disconnect cleanup and bounded FIFO fairness. Legacy wire still lacks exact response completion identifiers. | The engine's sync pump broadcasts `get-blocks` to every devnet peer; each answers a 512-block page |
| NET-16 | IMPLEMENTED | Whole-request metrics deadline. | Metrics server has no whole-request deadline (per-read timeout renews) |
| NET-17 | IMPLEMENTED | 8 MiB encoded page budget; first frame allowed for progress, frame cap still applies. | Devnet page has a block-count cap but no byte cap (unlike libp2p) |
| NET-19 | IMPLEMENTED | Private atomic fsynced identity-file creation under the existing node directory lock. | libp2p identity file is written non-atomically with the default umask before `chmod 0600`, and the chmod result is ignored |
| NET-20 | PARTIAL | Producer uses actual codec bytes plus current signer maximum before watermark/signing, retaining a deterministic attestation prefix below the gossip ceiling. Exact boundary and real transport payload tests pass. Incoming/historical oversized-block compatibility remains unresolved. | Frame-cliff vs consensus caps on the production transport |
| SR-01 | UNARMED CANDIDATE | The unpublished BPOSMAN2 candidate now folds the canonical manifest digest into its genesis mix, binding cohort membership and all other encoded network parameters into both the genesis block id and opening state root. Published BPOSMAN1 identity remains byte-frozen. Ceremony-tool reconciliation, independent review, an explicit network launch decision and publication of a new manifest remain required; no live network changed. See WAVE-47-SR01-GENESIS-COHORT-BINDING.md. | Genesis cohort is bound by neither the state root nor the genesis block id (only by the 32-bit `network_id`) |
| SR-04 | IMPLEMENTED | ws-verify enforces the boot arrangement window and refuses duplicate keys. | `ws-verify` diverges from the booting node: it omits the `arrangement_window` lower bound and carries stale duplicate-key text |
| SR-05 | PARTIAL | Full checkpoint retained and state root rechecked on canonical live apply/reorg before finality processing; conflicting evidence exits78. Missing/noncanonical evidence stays pending. Historical validator-set roots remain unverified. | A checkpoint's `state_root` / `validator_set_root` are never validated against the block they name, on either side |
| SR-06 | IMPLEMENTED | `single_derivation_path` now recursively scans all 29 Rust sources under `src/`, including nested transition modules, and normalizes whitespace before checking construction/impl/alias evasions. A regression pins nested coverage and spacing variants; production identity bytes are unchanged. | `single_derivation_path` has scan blind spots (currently clean) |
| SR-07 | IMPLEMENTED | Standalone verifier rejects zero/impossible thresholds and duplicate signer public keys; node and committee regressions. | `ws::verify_envelope` alone accepts a zero-threshold arrangement and does not compare signer keys; the crate relies on the node decoder for both |
| SR-08 | IMPLEMENTED | Decoder bounds counts by remaining bytes and shared attestation cap before allocation. | `codec::decode_envelope` pre-allocates from untrusted counts and hard-codes the attestation cap |
| SR-09 | IMPLEMENTED | Three ADR-041 commitment regressions pin existing encodings/zero semantics, mutate every key/value and test unique-entry order independence. Existing tag registry extended through 0x1E; production roots unchanged. | ADR-041 leaves (`0x1B`–`0x1E`) have no root-binding test, and the spec registry stops at `0x16` |
| ST-06 | UNARMED CANDIDATE | The rewards-v2 settlement now advances `issued_sat` for every delegator ledger credit; a forced-gate regression proves holdings growth equals minted issuance and the conservation predicate accepts the boundary. Pre-gate settlement remains inert and `REWARDS_V2_ACTIVATION_EPOCH` remains `u64::MAX`. Coordinated activation and full economics/replay qualification remain required. See WAVE-45-SUPPLY.md. | `close_epoch` mints delegator issuance shares into the ledger without advancing `issued_sat`; the supply-conservation rule would then refuse every bou… |
| ST-07 | UNARMED CANDIDATE | Same unarmed validator-duty candidate as FC-10. The candidate separates identical duties across genesis domains without changing current signatures. It does not yet make cross-network evidence invalid, bind keystore secrets to one network, or change the operator/devnet workflow; signing, validation, evidence rules and a coordinated epoch remain required. See WAVE-46-NETWORK-SIGNING-BINDING.md. | Attestation and proposal signing roots carry no network/genesis binding, so a validator key reused on another network (devnet, a fork with the same sl… |
| ST-08 | PROTOCOL DECISION | Cohort taper can concentrate power. Proposal: preserve stake-proportional weights; key count cannot establish operator independence. Existing economics not silently changed. | Cohort-cap `Deferred` threshold is a cliff: one 5% slash, an exit, or a leak-independent effective-stake dip of the sole independent validator below 2… |
| ST-09 | PARTIAL | Same admission-total correction as TX-05. | Mempool and consensus derive the funded-deposit stake cap from different totals (unleaked vs leak-applied roster) |
| TX-03 | PARTIAL | Mempool deduplicates semantic txid across encodings. Consensus bytes and historical identifiers unchanged. | Single-input transfers are cross-format malleable: a relay can re-encode V1↔V2 producing a distinct, valid `canonical_bytes` with the same `txid` |
| TX-04 | UNARMED CANDIDATE | Consensus and mempool candidate verification now fold `DS_SPEND2` with the committed genesis-manifest domain and fail closed at the rehearsed gate if it is absent. Below the still-inert `u64::MAX` gate, historical roots and states without a domain replay identically. Wallet/tool migration, pending-signature policy, mixed-fleet rehearsal and coordinated activation remain required; the old source label survives only as a compatibility API. See WAVE-46-NETWORK-SIGNING-BINDING.md. | `network_binding()` is a compile-time label, not a genesis-derived value, so the (inert) sighash binding does not actually separate networks built fro… |
| TX-05 | PARTIAL | Lifecycle admission and revalidation now use leak-adjusted active roster like current-state transition. Next-epoch changes still revalidate on-chain. | Mempool and consensus compute the funded-deposit cap from different `total_active` values (leak-applied vs unleaked) |
| TX-09 | UNARMED CANDIDATE | Rewards V2 stages leak-adjusted issuance, target/source-scoped participation, equivocation exclusion, delegated-stake/commission settlement, explicit delegator issuance accounting and a measurable withheld-proposal penalty. The ST-06 conservation defect in that candidate is now repaired. `REWARDS_V2_ACTIVATION_EPOCH` remains `u64::MAX`; full economic, replay, mixed-fleet and state-growth qualification plus an explicit activation decision remain required. See WAVE-46-TX09.md. | Issuance accounting below `REWARDS_V2`: delegators receive zero issuance, leak does not reduce income, credit is one unscoped bit, withheld proposals … |
| TX-10 | UNARMED CANDIDATE | The reference producer already stops at 256 transactions and the mempool at 4,096. Consensus now has a tested 4,096-count candidate behind the still-inert staking-metering gate; below it, historical verdicts are unchanged. Lifecycle activation also causes staking charges to consume gas/bytes from epoch 2884, but that coupling does not activate the dedicated gate. Coordinated activation, historical/non-reference-producer replay and resource qualification remain required. See WAVE-45-TX10.md. | Staking-class transactions are unmetered (0 gas, 0 bytes) and there is no consensus transaction-count cap |
| TX-13 | PARTIAL | The node now exports exact wall/head headroom to `MAX_EPOCH_ADVANCE` and emits rate-limited early, critical and zero-headroom warnings at 512, 128 and 0 epochs remaining. The calculation is saturating, node-local and never feeds block validity or duties, so replay and consensus are unchanged. The ceiling remains: the reference producer has no supported intermediate-slot recovery mode, and raising/removing the bound would reopen untrusted epoch-walk work or require a coordinated consensus release. See WAVE-46-TX13.md. | `MAX_EPOCH_ADVANCE = 4096` is a hard liveness ceiling: a network-wide stall longer than ~45.5 days can only be resumed by a hard fork |
| TX-14 | PARTIAL | The exact 1,600,000 BLOCH genesis offset is exposed by `supply_gap_sat`, pinned by regression, and capped so a future genesis cannot silently exceed it; manifest validation is fatal rather than warning-only. A new burn regression proves accounted holdings and the gap fall by the exact burn while gross `issued_sat` remains monotone. The historical offset is not repaired: doing so requires a genesis relaunch or an explicitly migrated committed counter, and redefining gross issuance as circulating supply is a protocol/economic decision. See WAVE-45-SUPPLY.md. | Genesis bonded 1,600,000 BLOCH outside `issued_sat`; the conservation invariant is a one-sided delta that can never see it, and burns are uncounted |
| TX-15 | PARTIAL | Byte-identical in-block attestation replays reuse the first successful verification without changing the pre-gate accepted-body verdict. Signature variants for the same voter/root are still verified so an invalid second signature remains a block reject. Pair-level refusal exists behind inert `ATTESTATION_DEDUP_ACTIVATION_EPOCH`; valid randomized variants remain residual until coordinated activation. | Duplicate `(validator, signing_root)` attestations cost one hybrid verify each and are accepted (idempotent) |
| TX-16 | IMPLEMENTED | Gossip admission now authenticates the fixed-size signed header before Merkle hashing or transaction decoding, and retains the decoded vector instead of parsing it twice. A forged-signature plus malformed-body regression pins the precedence; all 19 ingest-admission tests pass. Consensus repeats the authoritative checks on application. See WAVE-29.md. | Body decode, re-encode and Merkle hashing (steps 3b) run before the proposer signature (step 7); an unauthenticated peer can force ~8 MiB of hashing p… |
| BV-16 | PARTIAL | The feature is now explicitly documented as a color-changing escrow/representation bridge, not a chameleon hash or trapdoor. Returns bind route, adapter code hash, burn proof/count, PQ recipient signature, escrow backing and permanent nullifier; 13 state-machine/crypto tests pass. The host still supplies the trusted foreign checkpoint, no production finality verifier or consensus integration exists, and a compromised trust source can authorize false burns within locked backing. See WAVE-38.md. | Chameleon: no chameleon hash / trapdoor exists; the actual "forgery capability" belongs to whoever supplies `TrustedBurnCheckpoint`, and it is total (… |
| BV-17 | PARTIAL | A regression now executes the exact helper-produced guard and proves both the ECDSA and PQ legs are required. The historical eUTXO VM supports it, but native Ustav deliberately rejects every classical/ECDSA custody policy; no anchor integration or activation exists. See WAVE-37.md. | `hybrid_wbtc_validator` (the "Custody 2-of-2" anchor guard) is never executed in a test, and it cannot run on the PQ-only Ustav kernel the anchor is s… |
| BV-18 | IMPLEMENTED | Corrected the collected claims about PQ enforcement, recovery authorization after `r` is public, watchtower fee authority, deletion in the legacy shared-key construction, audit qualification, and implementation status. A blocking wording guard covers the known regressions in both CI systems; crate/API suites pass. Documentation correction only: no anchor registry, covenant, product deployment, or external audit is claimed. See WAVE-45-INF20-BV18.md. | Documentation overclaims relative to the code (collected) |
| BV-19 | PARTIAL | Fallible explicitly versioned V1/V2/V3 restore preserves existing outputs. Checked vault construction rejects null outpoints, reused/uncompressed roles and invalid sighash indexes. Old V1 convenience and unchecked transaction APIs retain compatibility contracts; consumers must choose safe APIs for untrusted input. | Minor robustness items in `bloch-btc-wallet` / `bloch-pq-vault` derivation |
| BV-20 | PARTIAL | Unknown/conflicting fields rejected; malformed-field errors no longer echo raw input and verify endpoint omits unnecessary copied public-key/commitment fields. Required public construction artifacts remain; arbitrary secret content cannot be detected. | `guard_no_secrets` inspects key names only; a secret placed in a free-form value passes and is echoed back; `deny_unknown_fields` is ineffective on th… |
| BV-21 | PARTIAL | Strict mock codec rejects noncanonical/overflow/trailing forms; outputs-only RPC no longer guesses consensus hex. Matching policy checks expected commitment and caller height/confirmations. Signer binding, authenticated inclusion/finality and consensus codec remain open. | `anchoring/` convention: first-`BLA1`-output heuristic, no signer binding, non-consensus tx codec |
| CR-09 | IMPLEMENTED | Fork README, NOTICE and VENDOR documentation now describe the actual build/dependency changes. Documentation correction only; authenticated upstream equivalence is not established. | Fork provenance documentation is inaccurate: README/NOTICE/VENDOR.toml claim only `src/lib.rs` changed and `build.rs` is identical to upstream; `build… |
| CR-10 | PARTIAL | Explicit raw and strict enveloped verifiers avoid magic sniffing when metadata is trusted. Vault/disclosure/Ustav, pool ownership, wallet messages, public `Keypair::verify`, deposit/funding checks, native funding-plan autocheck, payout inspection and deposit inspection expose explicit compatible/canonical policies. The wallet keypair API now tries enveloped/enveloped and raw/raw explicitly before its retained historical mixed fallback. Genuine pool-, wallet-, keypair-, deposit-, TransferV2-, funding-plan- and payout-domain raw `B1 0C` fixtures prove explicit raw verification avoids generic misclassification. Consensus and final historical fallbacks remain unchanged; custody/proof/miner inventories, default rollout and coordinated migration remain open. See WAVE-55-CR10.md, WAVE-61-CR02-DISCLOSURE-CANONICAL.md, WAVE-62-CR02-CLI-CANONICAL.md, WAVE-63-CR02-USTAV-CANONICAL.md, WAVE-64-CR10-MAGIC-PREFIX-FIXTURE.md, WAVE-65-CR10-POOL-AUTH.md, WAVE-66-CR10-WALLET-MESSAGE.md, WAVE-67-CR10-DEPOSIT-FUNDING.md, WAVE-68-CR10-FUNDING-VERIFY.md, WAVE-69-CR10-PAYOUT-INSPECT.md, WAVE-70-CR10-DEPOSIT-INSPECT.md, WAVE-71-CR10-NATIVE-FUNDING.md and WAVE-72-CR10-KEYPAIR-VERIFY.md. | Legacy raw-signature magic ambiguity (1/65536) remains for signatures |
| CR-11 | PARTIAL | Pinned official NIST ACVP ML-DSA-65 verification samples and Falcon round-3 submission vectors exercise the compiled backends and production wrappers. An official ACVP ML-DSA-65 key pair now also parses in the backend and interoperates with locally generated production-wrapper signatures. This is not a keygen/signing KAT or an official positive empty-context signature vector; those, all architectures and certification remain outside the evidence. See WAVE-56-CR11.md. | No standards-traceable KATs for ML-DSA-65 / Falcon-1024; seeded golden vectors pin the *crate*, not the *standard* |
| CR-12 | PARTIAL | New checked ASERT API rejects regressed heights, overflow and malformed/zero targets; unit/property/no_std checks pass. Historical consensus callers retain old behavior pending migration. | Genesis-3 PoW crate: `asert_next_bits` underflows on `new_height < anchor_height`; `bits_to_target` maps invalid compact bits to `Target::MIN` |
| EN-20 | IMPLEMENTED | One registry-identity helper serves production and tests, preserving consumed-reveal and generation-specific RANDAO checks. Nine registry gate tests and integrated node suite cover it. | Two implementations of the boot identity rule; the tested one has no production caller |
| EN-21 | IMPLEMENTED | Actual genesis validator index membership replaces dense-index assumption; sparse genesis identity and deposited-key retry regressions pass. | `genesis_validator_count` assumes dense manifest indices |
| EN-22 | PARTIAL | Finality-rewind, doppelgänger bypass and recovery/cache environment switches now accept only explicit `0/1/false/true`, are read once at boot and fail startup on empty, misspelled or otherwise ambiguous values. Three pure parser regressions pass. Deliberate safety overrides and KS-18's persistent-doppelgänger gap remain. See WAVE-32.md. | Env/flag-driven node-local safety behaviour (summary; see §3) |
| EN-23 | PARTIAL | Persisted validated restart caches and incremental state roots retained. Warm restore moves the validated cached prefix instead of cloning its envelopes; cache serialization borrows state and appends directly. Boot decoding streams one bounded frame, and cold replay now requires exact canonical parent continuity before applying each frame directly through the full transition; it no longer recomputes fork choice or scans branch pruning over every growing prefix. Reorg crash residues and index append gaps are repaired. Decoded canonical-history retention, state-size-dependent transition cost, peak RSS and production-scale recovery SLA remain unqualified. See WAVE-50-EN23.md and WAVE-51-EN23-LINEAR-REPLAY.md. | Per-ingest cost and memory grow with chain age; replay is quadratic |
| EN-24 | IMPLEMENTED | Same shared sync budget as NET-05. | Serving `get_blocks` is rate-limited per connection, not per peer/IP |
| FC-13 | PARTIAL | Latent 64-bit epoch-to-slice narrowing now fails closed on 32-bit targets. Boundary divergence observation no longer performs blocking stderr I/O inside consensus; a saturating atomic counter is exported as `bloch_pos_boundary_vote_drops_total`. Remaining aggregate notes include the seed fallback contract, exhaustion fallback bias, infallible canonical-encoding length casts and release-only invariant posture. See WAVE-25.md. | Arithmetic/panic notes (Info) |
| FC-14 | IMPLEMENTED | Protocol prose now distinguishes the inert F6 look-ahead from the live pre-gate seed rule, describes the partition committee rather than the retired 8/128 sampler, and identifies inactivity leak as quorum-weight accounting rather than a coin burn. Stale 2700 references now identify the 2880 replacement schedule. A source-prose regression pins these distinctions; the comment/constants guard remains clean. Slashing prose already records its source schedule and explicitly withholds fleet-deployment claims. See WAVE-26.md. | Documentation / spec divergences (Info) |
| INF-05 | REFUTED IN AUDIT | Auditor explicitly refuted bans failure; do not count as a fix. | `deny.toml` duplicate allowlist no longer matches `Cargo.lock`; the blocking `supply-chain` gate is very likely red — MEDIUM — NEW |
| INF-21 | IMPLEMENTED | Repository-owned workflow no longer assumes a personal checkout, retired architecture, fixed provider/model or already-missing features; it derives bounded review work from the current checkout and explicitly separates patches from activation/deployment. External workflow-host execution remains outside source verification. | A checked-in agent workflow with a stale, founder-specific context — INFO — NEW |
| INF-22 | PARTIAL | Corrected active/retired toolchain comments, retained Nix service identity/default RPC port, local-validation manifest warning and lock-integrity failure guidance. Broader operator-document drift, Nix evaluation and fleet inventory remain open. | Documentation rot that would mislead an operator — INFO — NEW |
| KS-13 | IMPLEMENTED | Keystore runbook now describes the supported explicit sealing command. | Stale documentation: `KEYSTORE-AT-REST.md` says "No re-seal tool" and "Option A is the only one an operator can actually execute" |
| KS-14 | IMPLEMENTED | Ceremony uses fresh inherited pipes rather than secret environment values; bounded/deadlined reader, closed descriptor, preallocated zeroizing buffers, disabled xtrace/allexport and PTY regressions. Legacy secret-environment API remains compatible outside this ceremony. | Ceremony script exports the passphrase into the environment of 64 child processes |
| KS-15 | PARTIAL | Drill creates a new private directory and unlinks only its four disposable secret files on normal/error/signal exits. SIGKILL/power loss and secure media erasure cannot be covered by shell cleanup. | `scripts/ws-ceremony-drill.sh` leaves throwaway signer `.sk` files in its work dir |
| KS-16 | PARTIAL | WS policy and malformed/configuration artifact refusals are typed exit 78; raw filesystem I/O remains ordinary failure. Supplied units suppress 78 and rate-limit other restarts; fleet deployment remains unverified. | WS boot refusal + `Restart=on-failure` is a replay-every-5-seconds crash loop |
| KS-17 | IMPLEMENTED | Plaintext opt-in consumed only as a recognized run/keygen option, never as another option value; regression test. | `--allow-plaintext-keystore` is matched anywhere in argv |
| KS-18 | PARTIAL | Detection now ignores pre-observation duties, covers held-attestation release and authenticated proposal sightings, starts a full window after replay/WS boot, blocks duties throughout observation and indefinitely after a live duplicate, and parses the opt-out strictly. Nine regressions pass. Detection state is still process-local/window-bound and an explicit operator bypass remains. See WAVE-35.md. | Doppelganger protection is in-memory, window-bounded and flag-bypassable |
| LG-09 | IMPLEMENTED | New sessions and historical journal replay canonicalize every valid address used by shares, block finders and snapshotted payouts. Case aliases merge into one derived accounting identity without rewriting the append-only source journal; an end-to-end replay regression proves shares and confirmed credits converge. See WAVE-43-LEGACY.md. | Pool: shares and ledger keyed by the raw `mining.authorize` username, not the parsed address |
| LG-10 | IMPLEMENTED | Terminal snapshot figures and deterministic 57-satoshi recipient are re-derived by an executable artifact test; the historical zero-value anchor is documented/tested; source and integration prose use the terminal vintage; the exporter reads the real selected-height index with an endianness regression. See WAVE-17.md. | Documentation drift on the ledger-critical code (dust recipient, snapshot vintage, zero-value rows, dead "tip height" print) |
| LG-11 | VERIFIED POSITIVE | The original audit classifies the advisor findings as verified closed, not as an open defect. Current `pool-proxy` tests pass 210/210 (207 unit + 3 integration) with local sockets. The PoW pool is idle/retired; its documented low/info residuals and deployment posture must be reviewed before any redeploy. See WAVE-40.md. | Pool/pool-proxy: advisor findings verified closed; residual notes for a redeploy |
| LG-12 | IMPLEMENTED | A fail-closed Cargo-graph guard proves the live `bloch-pos-node` package has no direct or transitive reachability to the retired `bloch`, `bloch-euvm` or `bloch-ffg` crates. Direct/transitive adversarial fixtures and a missing-root case prove the guard can fail; both blocking pipelines run it. See WAVE-31.md. | euvm / ffg reachability from the live node |
| LG-13 | PARTIAL | SP1 service preserves fixed-int proof encoding, rejects trailing/oversized proofs, authenticates before JSON and retains native worker permits after request timeout. Actual SDK unit build11tests passed; real guest/proof, spike/fuzz and funded authorization qualification remain open. | coherence-prover / SP1, spikes, fuzz |
| NET-21 | PARTIAL | Metrics remain off by default and loopback-bound; non-loopback binding requires standalone acknowledgement. The listener retains its 16-worker global ceiling, adds a four-worker cap per normalized source IP with RAII release, and makes overload responses nonblocking in the accept loop. This is local fairness, not identity: distributed sources can fill the global cap, NAT clients share four slots, and opt-in routable metrics still lack authentication/TLS and expose operational state. See WAVE-43-NETWORK.md, WAVE-50-NET21.md and WAVE-55-NET21-METRICS-ADMISSION.md. | Information exposure by design (Info) |
| NET-22 | PARTIAL | Both transports reserve source plus aggregate capacity before application decode; directed libp2p sync pages reserve per-peer bytes before each envelope decode and fail/page-gap safely on saturation or malformed input. RPC admission is bounded, and unindexed block-log serving stops after 4,096 frames pending restart/index rebuild. Block-log page scans and index rebuilds reuse one fixed-size header array per loop; append plus synchronous and asynchronous reorg rewrites preflight the exact canonical envelope length and stream the prefix and borrowed envelope fields without a store-owned whole-payload aggregate, while repair streams each derived index entry directly from the header-only scan through a fixed 8 KiB buffer, eliminating both whole-tail collections. Cold block-log replay and operator/offline-repair inspection now each reuse one raw-frame scratch allocation across complete frames after the unchanged cap, offset and truncation preflights; decoded envelopes own every field before replay retains them or inspection discards them. The scratch's logical high-water remains within the 8 MiB frame cap, while allocator capacity and RSS are not claimed. Skipped bodies remain seek-only and returned payload bytes/order are unchanged. A late local read/write fault may leave a disposable checked index prefix before open/rewrite fails; the log remains authoritative and the next repair truncates partial records. Each sync frame actually returned still needs an owned payload allocation; replay and inspection still read, overwrite and decode every complete raw frame; replay retains the final owned envelopes, while inspection still allocates and discards their owned decoded fields; header decode, file opens, index search, returned-body reads and the bounded unindexed tail remain. Libp2p still allocates its bounded outer frame, and IP/Host/source limits are fairness controls rather than authentication. See WAVE-89-NET22-FIXED-HEADER-BUFFERS.md, WAVE-95-NET22-STREAMED-BLOCK-LOG-APPENDS.md through WAVE-97-NET22-STREAMED-INDEX-SCAN.md, WAVE-117-NET22-STREAMED-ENVELOPE-PERSISTENCE.md, WAVE-141-NET22-REUSED-REPLAY-BUFFER.md, WAVE-144-NET22-REUSED-INSPECTION-BUFFER.md and the prior NET-22 reports. | Minor code-level notes (Info) |
| SR-10 | IMPLEMENTED | State-root documentation now accurately describes bounded thread-local RefCell memoization and its non-authoritative role; no production behavior changed. | `state_root.rs` doc claims "no global mutable state" while holding a thread-local two-generation memo (consensus-safe, doc false) |
| SR-11 | IMPLEMENTED | `params::DOMAIN_TAGS` is the complete 16-domain authority and records every live or explicitly unarmed candidate preimage shape. Frozen multi-shape domains document their marker/role/length/nested-domain separation without changing historical digest bytes. `DS_SPEND2` and the unarmed `DS_NETSIG2` are covered by the independent frozen guard and normative table. See WAVE-24.md and WAVE-46-NETWORK-SIGNING-BINDING.md. | Domain-tag hygiene: several tags cover two or three preimage shapes, docs are stale, and three tags live outside the registry |
| SR-12 | BASE CHANGED | Recovery/release base differs from audited commit: inspect activated lifecycle gates or TCP reconnect fix. Fleet binary inventory and historical epochs still require verification. | The weak-subjectivity window's slashability premise is void while slashing, exits and withdrawals are unarmed |
| SR-13 | PARTIAL | Same-prefix issuance remains immutable. Optional owned0700 publication directory coordinates network/genesis/epoch records across prefixes using fsynced atomic no-replace publication, including races. Independent registries, old tooling and omitted option remain outside protection. | Same-epoch re-mint is a permanent boot refusal for every node that stored the first artifact |
| ST-10 | IMPLEMENTED | Slashing module, decoder, transition, application, admission and release-test prose now state the scheduled epoch-2884 behavior and separate source reachability from deployment evidence. Lifecycle/leak comments were reconciled too; the automated constant guard reports zero contradictions. | Stale/contradictory comments on the slashing path |
| ST-11 | PARTIAL | The live-manifest regression now pins the exact 32-byte mainnet admission domain and the 1,600,000 BLCH launch principal derived from all 64 validator records. A manifest re-encoding, format drift, validator-set edit or stake edit now fails the release test instead of silently moving these immutable inputs. Historical state roots still do not commit either value, so a future commitment/versioning design and mixed-version replay plan remain required. See WAVE-45-ST11.md. | `genesis_principal_sat` and `admission_network_domain` are uncommitted, manifest-derived consensus inputs; the domain is `SHA3(manifest.encode())`, so… |
| ST-12 | IMPLEMENTED | The current validator-opening runbook now states that epoch 2,884 also makes lifecycle transactions consume the existing gas and byte budgets through the withdrawal-gate coupling, while charging no monetary fee and adding no count cap. A focused regression pins zero capacity charge immediately before the lifecycle boundary and non-zero gas/exact bytes at it without moving either activation constant. See WAVE-45-ST12.md. | Arming L also switches on gas/byte metering for ExitV2, RandaoRecommit and SlashingEvidence (`staking_tx_charge` reads `\ |
| ST-13 | UNARMED CANDIDATE | A single inert gate now couples a 4,096-entry lifetime registry ceiling with beacon-seeded ordering for funded applicants from the same deposit epoch. Below the gate, raw-pubkey ordering and accepted blocks remain unchanged. The seed is unknown when the key is committed eight epochs earlier, but FC-04 beacon influence and economically funded multi-key sampling remain residual. Cap policy, exhaustion recovery, legacy-unfunded ordering, replay and resource qualification remain required before activation. See WAVE-46-ST13.md. | Activation queue: intra-epoch order is grindable by `pubkey_hash` and the queue is unbounded (economically bounded by 25,000 BLCH per entry locked ≥ 2… |
| ST-14 | VERIFIED POSITIVE | Re-executed the blocking CI mutation guard: the unmodified control passed; all eight compiled mutations produced test failures; shipping source remained byte-identical. This is verification of an existing safeguard, not a remediation claim. See WAVE-23.md. | Mutation guard (`scripts/check-validator-lifecycle-mutations.py`) matches the code and each of its eight mutations is killed by `transition::tests::va… |
| ST-15 | PARTIAL | The node's evidence-submission log now states the actual economics: mempool admission/broadcast pays the observer nothing, and only the proposer of a block that includes the evidence may receive the protocol credit. A regression prevents the operator message from again implying observer payment. Front-running and reporter attribution remain protocol-design issues; no reward rule changed. See WAVE-47-ST15.md. | Whistleblower is always the *including proposer*; the observer who gossips evidence earns nothing, and evidence is trivially front-run |
| ST-16 | UNARMED CANDIDATE | Existing `ExitV2` can schedule the normal delayed withdrawal for a funded registration that remains queued after the activation delay, behind `FUNDED_VALIDATOR_CANCELLATION_ACTIVATION_EPOCH = u64::MAX`. The candidate preserves registered-key authorization, exact-epoch replay binding, shared exit churn, backed-principal accounting, and permanent registry/history tombstones; later finality cannot activate a cancelled record. Production behavior is unchanged. Activation still requires replay/mixed-fleet qualification and explicit cancellation-floor, churn-interaction and permanent-capacity policy decisions. See WAVE-48-ST16.md. | A queued funded validator whose deposit epoch is never finalized has no exit: `ExitV2` requires `activation_epoch <= epoch`, `Withdraw` requires an ex… |
| TX-17 | IMPLEMENTED | Funded decoding retains wire/resource bounds but no longer invokes the economic shape judge. Bounded invalid semantics decode and then fail `validate_shape`; node admission and committed-state validation still judge every deposit. Full committee suite passes; see WAVE-18.md. | `FundedDeposit::decode` enforces consensus rules (`validate_shape`) inside the decoder, contrary to the stated decode/judge split |
| TX-18 | IMPLEMENTED | Tag `0x04` retains its frozen eligibility byte, but transition validation now rejects caller-supplied `false` atomically and stores derived `true`. A regression pins codec compatibility and state ownership; full committee suite passes. See WAVE-19.md. | `Delegate.eligible` is a wire-supplied bit that consensus records verbatim |
| TX-19 | IMPLEMENTED | Fee-market scope/caps/base-fee tag/inflation and terminal tokenomics totals/digests/shares now match shipped constants. Two executable spec guards reject the audited stale claims. See WAVE-20.md. | Spec/code drift in `BLOCH-L1-FEE-MARKET.md` and `BLOCH-TOKENOMICS-V4.md` |
| TX-20 | IMPLEMENTED | `StateRoots` is explicitly a 14-field compatibility DTO; the live SMT exports one append-only 30-component registry consumed by uniqueness and spec tests. Reject precedence is documented as diagnostic/DoS behavior, not committed consensus data. See WAVE-21.md. | `interfaces.rs` `StateRoots` (14 fields, "closed again at eight components") vs `state_root.rs` (~25 tags) and the frozen `StateTransition::apply_bloc… |
| TX-21 | IMPLEMENTED | The drifted derive/produce stack was already deleted by `cabcdcee`, an ancestor of both the audited revision and remediation base. A source-structure regression now prevents its module/types/functions from silently returning, and production docs point to the node engine's shared `Transition` path. The real producer/self-validation root regression passes. See WAVE-22.md. | Two block-validation stacks (`derive::validate_block` vs `transition`) still coexist |
| TX-22 | PARTIAL | New genesis/genesis-mainnet CLI creation rejects duplicate validator indices/public keys and inconsistent cohorts before output. Historical decoding and committed-state constructors remain compatible; arbitrary programmatic callers still require separate policy. | `CommittedState::genesis` silently overwrites duplicate validator indices / pubkeys from the manifest |
| NET-18 | PARTIAL | Nix module requires explicit transport matching peers, wires devnet mesh options and validates dual ports. Wrong default/comment removed; NixOS evaluation/deployment remains unverified. | Deploy/config drift: NixOS PoS module defaults to `--transport libp2p`, which does not interoperate with the live fleet; its comment about the binary'… |
| TX-07 | PARTIAL | Live node admission caps declared size at canonical encoding plus the shared slack, and proposal selection budgets the consensus-declared size. Consensus has the same `OverdeclaredSize` rule and regressions behind inert `TX_BYTES_BOUND_ACTIVATION_EPOCH`; a hand-built body remains valid until a coordinated flag day. | Declared `tx_bytes` may exceed the encoding by any amount; one small transfer can fill the block byte budget |
| LD-01 | IMPLEMENTED | Root rustls 0.23.45 and rustls-webpki 0.103.15; configured cargo-audit passes. | Rustls advisory |
| LD-02 | PARTIAL | Updated inaccurate bincode rationale; cross-workspace exception pruning and stale-lock cleanup deferred pending all-lockfile verification. | Lockfile and advisory exception hygiene |
| LD-03 | IMPLEMENTED | The root dev/test profile now optimizes `bloch-sis-pow` while retaining debug assertions and overflow checks. The complete offline suite passed 81 tests (3 ignored) in 2m27s including recompilation, versus the audited 18m45s. Both CI timeout comments were reconciled. See WAVE-30.md. | Unoptimized legacy PoW test duration |
