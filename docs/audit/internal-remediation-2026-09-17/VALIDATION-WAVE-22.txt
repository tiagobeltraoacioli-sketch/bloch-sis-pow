Validation wave 22 — 2026-09-17
Branch: fix/internal-audit-20260917
Implementation: 5759d0e

PASS

git merge-base --is-ancestor cabcdcee b066e3c
git merge-base --is-ancestor cabcdcee 562e220
  Both exited 0. The commit that deleted derive/produce is present in the
  remediation base and the audited source.

cargo test --locked -p bloch-pos-committee --test spec_reconcile
  10 passed, 0 failed, including TX-21's retired-stack absence and live-engine
  call-site checks.

cargo test --locked -p bloch-pos-node \
  giving_a_block_back_leaves_the_head_header_root_equal_to_the_committed_state_root
  1 passed, 0 failed (503 node unit tests filtered). The test required an
  ephemeral local transport port and passed outside the restricted sandbox.

python3 scripts/check-comment-constants.py
  430 files; 565 resolvable constants; 46 claims; 0 contradictions.

bash scripts/ci-banned-words.sh
  Passed.

git diff --check
  Passed before the implementation commit.

LIMIT

This wave changes no block-validation rule, accepted set, state-root byte,
activation schedule or fleet binary. It adds regression evidence for the
single stack already present in the audited source and corrects stale prose.
